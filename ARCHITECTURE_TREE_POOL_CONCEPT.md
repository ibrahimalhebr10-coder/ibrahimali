# المعمارية الصحيحة: مفهوم خزان الأشجار (Tree Pool Architecture)

## 🎯 المبدأ الأساسي

**المزرعة ليست مسارًا وليست تجربة.**
**المزرعة = خزان أشجار فقط (Tree Pool).**

---

## 🌳 تعريف المزرعة (Farm) - ثابت

### ما هي المزرعة؟

المزرعة هي **خزان محايد** يحتوي على أشجار متاحة للسحب.

### تحتوي على:

✅ `farm_id` - معرف فريد
✅ `location` - الموقع
✅ `tree_types` - أنواع الأشجار المتاحة
✅ `available_trees` - عدد الأشجار المتاحة
✅ `total_trees` - إجمالي الأشجار

### لا تحتوي على:

❌ مسارات (agricultural / investment)
❌ مستخدمين
❌ تجربة عرض
❌ محتوى Experience Builder

### المزرعة = صامتة

المزرعة لا "تعرف" كيف ستُستخدم أشجارها.
المزرعة فقط تُخزن وتُقدم الأشجار للسحب.

---

## 🔀 المسارات (Paths) - منطق المنصة

المسارات هي **طريقة معاملة الأشجار المسحوبة من الخزان**.

### 🌱 المسار الزراعي (Agricultural Path)

**الوصف:**
- يسحب أشجاراً من المزرعة (الخزان)
- يعاملها كتجربة زراعية وانتفاع
- يُبني رحلة زراعية للمستخدم

**التركيز على:**
- النمو الزراعي
- الصور والتوثيق
- العمليات الزراعية
- مراحل نمو الشجرة
- ماذا تريد بمحصولك

**العنوان في الواجهة:** "محصولي الزراعي" أو "مزرعتي الزراعية"

---

### 💼 المسار الاستثماري (Investment Path)

**الوصف:**
- يسحب أشجاراً من **نفس المزرعة** (الخزان)
- يعاملها كأصل استثماري
- يُبني رحلة استثمارية للمستخدم

**التركيز على:**
- الأصول والعقود
- المدة والعوائد
- فرص التوسعة
- المنتجات والمخلفات (قيم فقط)
- النمو الاستثماري

**العنوان في الواجهة:** "محصولي الاستثماري" أو "مزرعتي الاستثمارية"

---

## 🔗 آلية السحب من الخزان

### التدفق الكامل:

```
1. المستخدم يتصفح المزارع المتاحة
   ↓
2. يختار مزرعة ويحدد عدد الأشجار
   ↓
3. يختار المسار: زراعي أو استثماري
   ↓
4. يتم إنشاء حجز (reservation):
   - user_id
   - farm_id (من أي خزان)
   - path_type (agricultural / investment)
   - total_trees
   - status (pending → active)
   ↓
5. عند تفعيل الحجز (status = active):
   - يتم إنقاص available_trees من المزرعة
   - يتم إنشاء سجل في المسار المُختار تلقائياً
   ↓
6. التجربة تُبنى حسب المسار
```

### ملاحظة حاسمة:

📌 **نفس المزرعة، سجل مختلف حسب المسار**

مثال:
- مستخدم A يحجز 10 أشجار من المزرعة X بمسار زراعي
- مستخدم B يحجز 15 شجرة من المزرعة X بمسار استثماري
- المزرعة X = نفسها (خزان واحد)
- التجربة = مختلفة تماماً

---

## 📊 البنية التقنية

### جدول `farms` (المزارع - الخزان)

```sql
CREATE TABLE farms (
  id uuid PRIMARY KEY,
  name_ar text,
  location text,
  tree_types jsonb,
  available_trees integer,  -- الأشجار المتاحة للسحب
  total_trees integer,       -- إجمالي الأشجار
  status text,
  -- ... معلومات أخرى
);
```

**تعليق:**
> المزرعة = خزان أشجار فقط (Tree Pool).
> ليست مسارًا، ليست تجربة، صامتة.
> الأشجار تُسحب منها عبر الحجوزات.

---

### جدول `reservations` (الحجوزات - عملية السحب)

```sql
CREATE TABLE reservations (
  id uuid PRIMARY KEY,
  user_id uuid,
  farm_id uuid,              -- من أي خزان سُحبت
  path_type text,            -- agricultural أو investment ← المفتاح!
  total_trees integer,
  total_price decimal,
  status text,               -- pending, active, completed, cancelled
  created_at timestamptz,
  -- ...
);
```

**تعليق على `path_type`:**
> نوع المسار المُختار عند الحجز: agricultural (زراعي) أو investment (استثماري).
> هذا يحدد كيف ستُعامل الأشجار المسحوبة من خزان المزرعة.

**القاعدة:**
- `path_type = 'agricultural'` → تُبنى تجربة زراعية
- `path_type = 'investment'` → تُبنى تجربة استثمارية

---

### جدول `investment_agricultural_assets` (الأصول الاستثمارية)

```sql
CREATE TABLE investment_agricultural_assets (
  id uuid PRIMARY KEY,
  user_id uuid,
  farm_id uuid,              -- من أي خزان (من الحجز)
  contract_id uuid,          -- ← يشير إلى reservation_id
  tree_type text,
  quantity integer,
  acquisition_date date,
  notes text,
  -- ...
);
```

**تعليق على `contract_id`:**
> معرف الحجز (reservation_id) الذي سُحبت منه الأشجار.
> المصدر الأساسي للأصل. يجب أن يشير دائماً إلى حجز استثماري.

**تعليق على `farm_id`:**
> معرف المزرعة (الخزان) الذي سُحبت منه الأشجار.
> يأتي من الحجز. المزرعة = خزان محايد فقط.

**آلية الإنشاء:**
يتم إنشاء السجلات في هذا الجدول **تلقائياً** عند تفعيل حجز استثماري:

```sql
-- Trigger تلقائي
WHEN reservation.status = 'active'
AND reservation.path_type = 'investment'
→ إنشاء سجل في investment_agricultural_assets
```

---

### View: `investment_assets_with_reservation`

```sql
CREATE VIEW investment_assets_with_reservation AS
SELECT
  ia.*,
  r.status as reservation_status,
  r.path_type,
  r.total_price as contract_total_price,
  f.name_ar as farm_name,
  f.location as farm_location
FROM investment_agricultural_assets ia
LEFT JOIN reservations r ON ia.contract_id = r.id
LEFT JOIN farms f ON ia.farm_id = f.id;
```

**الفائدة:**
يوضح العلاقة الكاملة: **خزان → حجز → أصل → تجربة**

---

## 🧑‍🌾 زر "مزرعتي" في الواجهة (Frontend)

### ما يفعله الزر:

❌ **لا** يذهب إلى المزرعة
❌ **لا** يعرض خزان الأشجار
✅ **يذهب** إلى تجربة المسار الخاصة بالمستخدم

### حسب المسار:

#### 🌱 المستخدم في المسار الزراعي:
- يرى: رحلة أشجاره الزراعية
- يقرأ من: `agricultural_experience_content`
- الأشجار مصدرها: `reservations` حيث `path_type = 'agricultural'`

#### 💼 المستخدم في المسار الاستثماري:
- يرى: أصوله الاستثمارية
- يقرأ من: `investment_experience_content`
- الأشجار مصدرها: `reservations` حيث `path_type = 'investment'`

### كيف يعرف النظام المسار؟

المسار محفوظ في جدول `reservations.path_type`.
الواجهة تقرأ الحجوزات النشطة للمستخدم وتعرض التجربة حسب `path_type`.

**المزرعة تُجلب تلقائياً عبر الربط، ولا تُختار يدوياً في زر "مزرعتي".**

---

## 🧰 قسم "مزرعتي" في لوحة التحكم (Admin Dashboard)

### ⚠️ ما لا يفعله هذا القسم:

❌ **لا** يدير المزارع (هذا في قسم "إدارة المزارع")
❌ **لا** ينشئ أشجار
❌ **لا** يسحب من الخزان
❌ **لا** ينشئ حجوزات

### ✅ وظيفته الوحيدة:

**إدارة طريقة عرض وتجربة الأشجار المسحوبة حسب المسار.**

أي: يدير **Experience Builder** فقط.

### المسار الزراعي في الأدمن:

التبويبات:
1. خريطة الأشجار - **تعرض** الأشجار المسحوبة من الحجوزات الزراعية
2. العمليات الزراعية - **يسجل** العمليات على الأشجار
3. التوثيق الزراعي - **يسجل** الصور والمراحل
4. مراحل النمو - **يبني** رحلة النمو
5. **Experience Builder - يصيغ التجربة النهائية**

### المسار الاستثماري في الأدمن:

التبويبات:
1. الأصول الزراعية - **تعرض** الأشجار المسحوبة من الحجوزات الاستثمارية
2. حالة الاستثمار - **يسجل** حالة الاستثمار
3. المنتجات والمخلفات - **يسجل** القيم فقط
4. فرص التوسعة - **يبني** فرص النمو
5. **Experience Builder - يصيغ التجربة النهائية**

### القاعدة الذهبية:

🌟 **قسم "مزرعتي" يُصيغ التجربة، لا يُنشئ الأصول.**

الأصول تأتي من:
1. الحجوزات (reservations)
2. Triggers تلقائية عند تفعيل الحجوزات

قسم "مزرعتي" يُزين ويُنظم فقط.

---

## 📍 العلاقة النهائية (مختصرة)

```
┌──────────────────┐
│  إدارة المزارع   │ ← يدير الخزان (available_trees، total_trees)
└──────────────────┘
         ↓
         ↓ السحب عبر الحجوزات
         ↓
┌──────────────────┐
│   المسارات       │ ← تسحب من الخزان حسب path_type
└──────────────────┘
         ↓
         ↓ البناء التلقائي
         ↓
┌──────────────────┐
│ قسم مزرعتي (أدمن)│ ← يصيغ التجربة حسب المسار
└──────────────────┘
         ↓
         ↓ Experience Builder
         ↓
┌──────────────────┐
│  زر مزرعتي (UI) │ ← يعرض التجربة فقط
└──────────────────┘
```

---

## ⚠️ تنبيه نهائي - أخطاء شائعة يجب تجنبها

### ❌ خطأ 1: ربط المزرعة بمسار

```javascript
// خطأ!
farm.path_type = 'agricultural';
farm.is_investment = true;
```

**لماذا خطأ؟**
المزرعة محايدة. لا تنتمي لمسار.

**الصحيح:**
```javascript
// عند الحجز
reservation.path_type = 'agricultural'; // ← المسار يُختار هنا
reservation.farm_id = farm.id;          // ← المزرعة مصدر فقط
```

---

### ❌ خطأ 2: جعل المزرعة زراعية أو استثمارية

```sql
-- خطأ!
ALTER TABLE farms ADD COLUMN farm_type text;
UPDATE farms SET farm_type = 'investment' WHERE id = X;
```

**لماذا خطأ؟**
المزرعة = خزان. يمكن أن يسحب منها مستخدم زراعي ومستخدم استثماري في نفس الوقت.

**الصحيح:**
المسار في الحجز، ليس في المزرعة:
```sql
SELECT * FROM reservations WHERE farm_id = X AND path_type = 'agricultural';
SELECT * FROM reservations WHERE farm_id = X AND path_type = 'investment';
-- نفس المزرعة، مسارات مختلفة!
```

---

### ❌ خطأ 3: خلط السجل الزراعي والاستثماري

```sql
-- خطأ!
SELECT * FROM agricultural_operations
WHERE user_id = X AND path_type = 'investment';
```

**لماذا خطأ؟**
كل مسار له جداوله الخاصة. لا خلط!

**الصحيح:**
```sql
-- للزراعي
SELECT * FROM agricultural_operations WHERE user_id = X;

-- للاستثماري
SELECT * FROM investment_products_yields WHERE user_id = X;
```

---

### ❌ خطأ 4: السحب المباشر من المزرعة في الأدمن

```javascript
// خطأ في قسم "مزرعتي"!
createAsset({
  user_id: userId,
  farm_id: farmId, // ← سحب مباشر!
  quantity: 10
});
```

**لماذا خطأ؟**
قسم "مزرعتي" لا يسحب من الخزان. السحب يتم عبر الحجوزات فقط.

**الصحيح:**
```javascript
// في قسم الحجوزات (ليس مزرعتي)
createReservation({
  user_id: userId,
  farm_id: farmId,
  path_type: 'investment', // ← المسار
  total_trees: 10
});
// ثم عند تفعيل الحجز، يُنشأ الأصل تلقائياً via trigger
```

---

## ✅ ملخص القواعد

### 1. المزرعة
- ✅ خزان أشجار فقط
- ✅ محايدة، صامتة
- ❌ ليست زراعية أو استثمارية

### 2. الحجز
- ✅ عملية السحب من الخزان
- ✅ يحتوي على `path_type`
- ✅ المصدر الأساسي للأصول

### 3. المسارات
- ✅ تسحب من الخزان عبر الحجوزات
- ✅ كل مسار له جداوله الخاصة
- ❌ لا خلط بين المسارات

### 4. قسم "مزرعتي" (أدمن)
- ✅ يصيغ التجربة فقط
- ❌ لا يسحب أشجار
- ❌ لا ينشئ حجوزات

### 5. زر "مزرعتي" (واجهة)
- ✅ يعرض التجربة حسب المسار
- ❌ لا يذهب للمزرعة مباشرة

---

## 🎬 الخلاصة

**المعادلة:**
```
المزرعة = خزان
  ↓ (عبر الحجز + اختيار المسار)
الأشجار المسحوبة
  ↓ (بناء تلقائي)
الأصول/السجلات
  ↓ (Experience Builder)
التجربة النهائية
  ↓
زر "مزرعتي" في الواجهة
```

**أي تطوير يخالف هذه المعمارية يُعتبر خاطئًا ويجب إيقافه فوراً.**

---

تم التنفيذ: 2025-02-03
الحالة: ✅ مكتمل ومُطبق في قاعدة البيانات
