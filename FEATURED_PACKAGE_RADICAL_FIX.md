# الإصلاح الجذري للباقة المميزة وحقل كود المؤثر

## المشاكل التي كانت موجودة

### 1. الباقة المميزة تظهر دائماً ولا تختفي

**الأعراض:**
- الباقة المميزة تظهر تلقائياً عند فتح الصفحة
- لا تختفي عند إعادة تحميل الصفحة (F5)
- تبقى ظاهرة حتى بدون إدخال كود

**السبب الجذري:**
```typescript
// في AgriculturalFarmPage.tsx و InvestmentFarmPage.tsx
useEffect(() => {
  const storedCode = sessionStorage.getItem('influencer_code');
  if (storedCode) {
    setInfluencerCode(storedCode); // ❌ تحميل تلقائي!
  }
}, []);
```

**المشكلة:**
- الكود يُخزن في `sessionStorage` عند إدخاله
- عند إعادة فتح الصفحة، يُحمل الكود تلقائياً
- هذا يجعل `influencerCode` موجود دائماً
- وبالتالي `isFeatured = influencerCode && index === 0` يكون `true` دائماً

---

### 2. حقل إدخال كود المؤثر يختفي

**الأعراض:**
- بعد إدخال الكود، يختفي حقل الإدخال
- المستخدم لا يستطيع تغيير الكود
- لا يمكن إدخال كود آخر

**السبب الجذري:**
```typescript
// في AgriculturalFarmPage.tsx و InvestmentFarmPage.tsx
{!influencerCode && ( // ❌ شرط خاطئ!
  <div className="mt-3 mx-4">
    <InfluencerCodeInput ... />
  </div>
)}
```

**المشكلة:**
- الحقل يظهر فقط إذا لم يكن هناك `influencerCode`
- بمجرد إدخال الكود، يختفي الحقل
- لا يوجد طريقة لمسح الكود أو تغييره

---

### 3. خطأ RLS في جدول lead_activities

**الأعراض:**
```
POST .../lead_activities?select=* 401 (Unauthorized)
Error: new row violates row-level security policy
```

**السبب الجذري:**
```sql
-- السياسة القديمة
CREATE POLICY "Anyone can insert activities"
  ON lead_activities FOR INSERT
  WITH CHECK (true); -- ❌ فقط لـ authenticated!
```

**المشكلة:**
- السياسة لا تسمح للمستخدمين غير المسجلين (anon)
- الزوار لا يستطيعون تسجيل أنشطتهم
- نظام تتبع العملاء لا يعمل للزوار

---

## الحلول الجذرية المطبقة

### 1. إزالة التحميل التلقائي من sessionStorage

**التغيير في `AgriculturalFarmPage.tsx` و `InvestmentFarmPage.tsx`:**

#### قبل:
```typescript
useEffect(() => {
  const storedCode = sessionStorage.getItem('influencer_code');
  if (storedCode) {
    setInfluencerCode(storedCode); // ❌ تحميل تلقائي
  }
}, []);

useEffect(() => {
  const handlePageHide = () => {
    setShowFeaturedPackage(false);
    sessionStorage.removeItem('featured_package_active');
  };

  window.addEventListener('pagehide', handlePageHide);
  return () => window.removeEventListener('pagehide', handlePageHide);
}, []);
```

#### بعد:
```typescript
// ✅ لا تحميل تلقائي!

useEffect(() => {
  const handleBeforeUnload = () => {
    sessionStorage.removeItem('influencer_code');
    sessionStorage.removeItem('influencer_activated_at');
    sessionStorage.removeItem('featured_package_active');
  };

  window.addEventListener('beforeunload', handleBeforeUnload);
  window.addEventListener('pagehide', handleBeforeUnload);

  return () => {
    window.removeEventListener('beforeunload', handleBeforeUnload);
    window.removeEventListener('pagehide', handleBeforeUnload);
    handleBeforeUnload(); // ✅ تنظيف عند unmount
  };
}, []);
```

**النتيجة:**
- لا يتم تحميل الكود تلقائياً عند فتح الصفحة
- يتم مسح الكود من sessionStorage عند:
  - إعادة تحميل الصفحة
  - الخروج من الصفحة
  - إغلاق الصفحة
- الباقة المميزة تظهر فقط عند إدخال الكود في الجلسة الحالية

---

### 2. جعل حقل الكود ظاهراً دائماً

**التغيير في `AgriculturalFarmPage.tsx` و `InvestmentFarmPage.tsx`:**

#### قبل:
```typescript
{!influencerCode && ( // ❌ يختفي بعد الإدخال
  <div className="mt-3 mx-4">
    <InfluencerCodeInput ... />
  </div>
)}
```

#### بعد:
```typescript
{/* Influencer Code Input - Always Visible */}
<div className="mt-3 mx-4">
  <InfluencerCodeInput ... /> {/* ✅ ظاهر دائماً */}
</div>
```

**النتيجة:**
- حقل الكود ظاهر دائماً
- المستخدم يستطيع إدخال كود في أي وقت
- يمكن تغيير الكود أو مسحه

---

### 3. تحسين مكون InfluencerCodeInput

**التغييرات في `InfluencerCodeInput.tsx`:**

#### إضافة State جديد:
```typescript
const [hasEnteredCode, setHasEnteredCode] = useState(false);
```

#### إضافة دالة مسح الكود:
```typescript
const handleReset = () => {
  setInputValue('');
  setEnteredName('');
  setHasEnteredCode(false);
  sessionStorage.removeItem('influencer_code');
  sessionStorage.removeItem('influencer_activated_at');
  sessionStorage.removeItem('featured_package_active');
};
```

#### واجهة ذكية:

**قبل إدخال الكود:**
```
┌──────────────────────────────────────────┐
│ 🎁 هل لديك كود من شريك المسيرة؟         │
│ أدخل الكود للحصول على باقة مميزة         │
│                                          │
│ [_____________] [إدخال]                 │
│ ✓ يمكنك إدخال الكود في أي وقت           │
└──────────────────────────────────────────┘
```

**بعد إدخال الكود:**
```
┌──────────────────────────────────────────┐
│ 🎁 هل لديك كود من شريك المسيرة؟  [مسح]  │
│ تم تفعيل الكود: احمد_المزارع             │
│                                          │
│ ✓ تم تفعيل الباقة المميزة!              │
│   تحقق من العرض أدناه                    │
└──────────────────────────────────────────┘
```

**المميزات:**
- رسالة واضحة عن حالة الكود
- زر "مسح" لإزالة الكود
- رسالة تأكيد بعد التفعيل
- واجهة تتغير حسب الحالة

---

### 4. إصلاح RLS Policy لجدول lead_activities

**التغيير في قاعدة البيانات:**

#### Migration: `fix_lead_activities_rls_anon.sql`

```sql
-- حذف السياسة القديمة
DROP POLICY IF EXISTS "Anyone can insert activities" ON lead_activities;

-- إنشاء سياسة جديدة تسمح لـ authenticated + anon
CREATE POLICY "Authenticated and anonymous can insert activities"
  ON lead_activities FOR INSERT
  TO authenticated, anon -- ✅ كلاهما!
  WITH CHECK (true);
```

**النتيجة:**
- المستخدمون المسجلون يستطيعون تسجيل الأنشطة
- الزوار (anon) يستطيعون تسجيل الأنشطة
- لا مزيد من أخطاء 401 Unauthorized
- نظام تتبع العملاء يعمل للجميع

---

## التدفق الجديد الصحيح

### السيناريو 1: مستخدم يفتح صفحة مزرعة

```
1. المستخدم يفتح صفحة مزرعة (زراعية أو استثمارية)
   └─ influencerCode = null
   └─ showFeaturedPackage = false

2. يظهر حقل إدخال الكود
   └─ "هل لديك كود من شريك المسيرة؟"

3. المستخدم يدخل كود: "احمد"
   └─ hasEnteredCode = true
   └─ influencerCode = "احمد"
   └─ showFeaturedPackage = true
   └─ sessionStorage.setItem('influencer_code', 'احمد')

4. تظهر رسالة تهنئة
   └─ "مبروووك! 🎉"

5. يتم إخفاء رسالة التهنئة
   └─ الحقل يظهر رسالة: "تم تفعيل الكود: احمد"
   └─ زر "مسح" يظهر

6. تظهر الباقة المميزة (FeaturedPackageOverlay)
   └─ فوق الباقات العادية

7. الباقة الأولى في السلايدر تُصنف كـ "مميزة"
   └─ isFeatured = true
   └─ تصميم ذهبي خاص
   └─ "+6 أشهر إضافية مجاناً"
```

---

### السيناريو 2: مستخدم يعيد تحميل الصفحة

```
1. المستخدم يضغط F5 (إعادة تحميل)

2. beforeunload event يُطلق
   └─ sessionStorage.removeItem('influencer_code')
   └─ sessionStorage.removeItem('influencer_activated_at')
   └─ sessionStorage.removeItem('featured_package_active')

3. الصفحة تُحمل من جديد
   └─ influencerCode = null ✅
   └─ showFeaturedPackage = false ✅
   └─ hasEnteredCode = false ✅

4. حقل الكود يظهر في الحالة الافتراضية
   └─ "هل لديك كود من شريك المسيرة؟"

5. لا توجد باقة مميزة ✅
   └─ الباقات العادية فقط
```

---

### السيناريو 3: مستخدم يريد مسح الكود

```
1. المستخدم أدخل كود "احمد"
   └─ حقل الكود يظهر: "تم تفعيل الكود: احمد" [مسح]

2. المستخدم يضغط زر "مسح"
   └─ handleReset() يُنفذ

3. الحالة تُعاد إلى الافتراضية
   └─ inputValue = ''
   └─ enteredName = ''
   └─ hasEnteredCode = false
   └─ sessionStorage.clear()

4. حقل الكود يعود للحالة الافتراضية
   └─ "هل لديك كود من شريك المسيرة؟"
   └─ حقل إدخال فارغ

5. المستخدم يستطيع إدخال كود جديد ✅
```

---

## الملفات المعدلة

### 1. قاعدة البيانات

```
supabase/migrations/
  └─ fix_lead_activities_rls_anon.sql (جديد)
```

**التغييرات:**
- إصلاح RLS policy لـ lead_activities
- السماح لـ authenticated + anon بالإضافة

---

### 2. صفحات المزارع

```
src/components/
  ├─ AgriculturalFarmPage.tsx (معدل)
  └─ InvestmentFarmPage.tsx (معدل)
```

**التغييرات:**
- إزالة التحميل التلقائي من sessionStorage
- تحسين cleanup عند الخروج
- جعل حقل الكود ظاهراً دائماً
- إزالة شرط `!influencerCode`

---

### 3. مكون حقل الكود

```
src/components/
  └─ InfluencerCodeInput.tsx (معدل)
```

**التغييرات:**
- إضافة state `hasEnteredCode`
- إضافة دالة `handleReset()`
- واجهة ذكية تتغير حسب الحالة
- زر "مسح" لإعادة تعيين الكود
- رسائل واضحة للمستخدم

---

## الاختبار

### 1. اختبار الباقة المميزة

```
✅ اختبر:
1. افتح صفحة مزرعة (زراعية أو استثمارية)
2. تأكد من أن الباقات العادية فقط تظهر (لا باقة مميزة)
3. أدخل كود مؤثر (مثال: "احمد")
4. تأكد من ظهور رسالة التهنئة
5. تأكد من ظهور الباقة المميزة (FeaturedPackageOverlay)
6. تأكد من أن الباقة الأولى في السلايدر ذهبية
7. اضغط F5 (إعادة تحميل)
8. تأكد من اختفاء الباقة المميزة ✅
9. تأكد من عودة الحقل للحالة الافتراضية ✅
```

---

### 2. اختبار حقل الكود

```
✅ اختبر:
1. افتح صفحة مزرعة
2. تأكد من ظهور حقل الكود
3. أدخل كود "احمد"
4. تأكد من تغير الواجهة إلى "تم تفعيل الكود: احمد"
5. تأكد من ظهور زر "مسح"
6. اضغط زر "مسح"
7. تأكد من عودة الحقل للحالة الافتراضية
8. أدخل كود آخر "محمد"
9. تأكد من قبول الكود الجديد ✅
```

---

### 3. اختبار RLS

```
✅ اختبر:
1. افتح console في المتصفح
2. تأكد من عدم ظهور أخطاء:
   - ❌ POST .../lead_activities 401
   - ❌ row-level security policy
3. افتح صفحة مزرعة
4. في console، تأكد من ظهور:
   - ✅ [Lead Tracking] Page view tracked
   - ✅ [Lead Tracking] Farm view tracked
5. لا أخطاء 401 Unauthorized ✅
```

---

## الخلاصة

### المشاكل التي تم إصلاحها:

1. **الباقة المميزة تظهر دائماً**
   - ✅ السبب: تحميل تلقائي من sessionStorage
   - ✅ الحل: إزالة التحميل التلقائي + cleanup

2. **حقل الكود يختفي**
   - ✅ السبب: شرط `!influencerCode`
   - ✅ الحل: حقل ظاهر دائماً + زر مسح

3. **خطأ RLS 401**
   - ✅ السبب: سياسة لا تسمح لـ anon
   - ✅ الحل: تحديث السياسة لـ authenticated + anon

---

### السلوك الجديد الصحيح:

```
✅ الباقة المميزة:
- تظهر فقط عند إدخال كود
- تختفي عند إعادة التحميل
- تختفي عند مسح الكود
- عنصر تسويقي مؤقت (كما هو مطلوب)

✅ حقل الكود:
- ظاهر دائماً
- واجهة ذكية (قبل/بعد الإدخال)
- زر مسح للتراجع
- يمكن تغيير الكود

✅ تتبع العملاء:
- يعمل للمسجلين
- يعمل للزوار (anon)
- لا أخطاء في console
- نظام Hot Leads يعمل

✅ البناء:
- بدون أخطاء TypeScript
- بدون تحذيرات
- جاهز للإنتاج
```

---

## للمستقبل

### نصائح للتطوير:

1. **لا تستخدم sessionStorage للباقة المميزة:**
   - الباقة المميزة عنصر مؤقت
   - يجب أن تختفي عند إعادة التحميل
   - لا تخزن حالتها

2. **الحقول المهمة يجب أن تكون ظاهرة:**
   - لا تخفي حقول الإدخال
   - أضف زر مسح/تعديل
   - اجعل الواجهة واضحة

3. **RLS Policies:**
   - تذكر anon للزوار
   - authenticated للمسجلين
   - ادمج الاثنين عند الحاجة

---

تاريخ الإصلاح: 5 فبراير 2026
الحالة: ✅ **مكتمل وجاهز**
البناء: ✅ **نجح بدون أخطاء**
