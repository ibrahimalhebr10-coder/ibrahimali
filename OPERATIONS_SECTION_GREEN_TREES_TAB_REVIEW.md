# مراجعة تنفيذ تبويب "أشجاري الخضراء" داخل قسم التشغيل

التاريخ: 4 فبراير 2026
الحالة: مراجعة كاملة وتقييم دقيق

---

## أولاً: الإجابات المباشرة على الأسئلة العشرة

### 1️⃣ هل تم إنشاء تبويب مستقل باسم "أشجاري الخضراء" داخل قسم التشغيل؟

**⬜ لا**

**التوضيح:**
- تم إنشاء قسم **"التشغيل"** (`OperationsSection.tsx`) كقسم مستقل في لوحة الإدارة
- **لكن** لا يوجد تبويب باسم "أشجاري الخضراء" داخل هذا القسم
- القسم يحتوي على نموذج إدخال صيانة واحد بدون أي تبويبات فرعية باسم "أشجاري الخضراء"

**المسار الفعلي:**
- `/src/components/admin/OperationsSection.tsx`
- هذا المكون يُعرض كقسم كامل وليس كتبويب

**الفرق المطلوب:**
- المطلوب: تبويب منفصل باسم "أشجاري الخضراء" داخل قسم التشغيل
- الموجود: قسم تشغيل واحد يحتوي على نموذج إدخال صيانة بدون تصنيف لـ "أشجاري الخضراء"

---

### 2️⃣ هل تبويب أشجاري الخضراء يعمل على مستوى:

**✅ المزرعة**

**التوضيح:**
- النظام المُنفذ **يعمل على مستوى المزرعة فقط**
- جدول `maintenance_records` يحتوي على `farm_id` كـ foreign key مباشر
- كل سجل صيانة مرتبط بمزرعة واحدة
- النظام لا يرتبط بالمستخدمين مباشرة (صحيح حسب المواصفات)

**الدليل من قاعدة البيانات:**
```sql
CREATE TABLE maintenance_records (
  id uuid PRIMARY KEY,
  farm_id uuid NOT NULL REFERENCES farms(id) ON DELETE CASCADE,
  -- لا يوجد user_id هنا ✅
  ...
);
```

---

### 3️⃣ هل يوجد داخل التبويب نموذج إضافة صيانة مرتبط بمزرعة؟

**✅ موجود كامل**

**التوضيح:**
النموذج يحتوي على:
- ✅ **اختيار المزرعة** (dropdown يعرض جميع المزارع مع عدد الأشجار)
- ✅ **نوع الصيانة** (periodic/seasonal/emergency)
- ✅ **تاريخ التنفيذ** (maintenance_date)
- ✅ **حالة** (draft/published/completed)

**الموقع في الكود:**
```tsx
// OperationsSection.tsx - lines 284-337
<form onSubmit={handleCreateRecord}>
  <select name="farm_id">...</select>
  <select name="maintenance_type">...</select>
  <input type="date" name="maintenance_date">...</input>
  <select name="status">...</select>
</form>
```

---

### 4️⃣ هل يدعم نموذج الصيانة إضافة مراحل متعددة؟

**✅ نعم**

**التوضيح:**
النظام يدعم إضافة مراحل متعددة لكل صيانة، وكل مرحلة تحتوي على:
- ✅ **اسم المرحلة** (stage_title)
- ✅ **تعليق** (stage_note)
- ✅ **تاريخ** (stage_date)

**الدليل من قاعدة البيانات:**
```sql
CREATE TABLE maintenance_stages (
  id uuid PRIMARY KEY,
  maintenance_id uuid NOT NULL,
  stage_title text NOT NULL,
  stage_note text,
  stage_date date NOT NULL,
  ...
);
```

**الموقع في الكود:**
```tsx
// OperationsSection.tsx - lines 466-530
// تبويب "المراحل" يسمح بإضافة مراحل متعددة
```

---

### 5️⃣ هل يدعم التبويب رفع صور وفيديو؟

**✅ صور + فيديو**

**التوضيح:**
- النظام يدعم رفع **صور وفيديو** من الجوال والكمبيوتر
- المرفقات **مرتبطة بسجل الصيانة** (maintenance_id)
- يتم تخزينها في Supabase Storage bucket: `maintenance-media`

**الدليل من قاعدة البيانات:**
```sql
CREATE TABLE maintenance_media (
  id uuid PRIMARY KEY,
  maintenance_id uuid NOT NULL REFERENCES maintenance_records(id),
  media_type text CHECK (media_type IN ('image', 'video')),
  file_path text NOT NULL,
  ...
);
```

**الموقع في الكود:**
```tsx
// OperationsSection.tsx - lines 533-601
// تبويب "الملفات" يسمح برفع صور وفيديو
<input type="file" accept="image/*" />
<input type="file" accept="video/*" />
```

---

### 6️⃣ هل يوجد في نفس نموذج الصيانة خيار إضافة رسوم صيانة؟

**✅ نعم**

**التوضيح:**
يوجد تبويب منفصل داخل تفاصيل الصيانة لإضافة رسوم:
- ✅ **إدخال مبلغ إجمالي** (total_amount)
- ✅ **تقسيم تلقائي** على عدد أشجار المزرعة

**الموقع في الكود:**
```tsx
// OperationsSection.tsx - lines 604-651
// تبويب "رسوم الصيانة"
<input type="number" placeholder="أدخل المبلغ الإجمالي" />
```

---

### 7️⃣ هل يتم حساب تكلفة الشجرة تلقائياً؟

**✅ نعم**

**التوضيح:**
- يتم حساب تكلفة الشجرة **تلقائياً** عند إضافة/تحديث رسوم الصيانة
- الحساب: `cost_per_tree = total_amount ÷ farm.total_trees`
- يتم التخزين في: `maintenance_fees.cost_per_tree`

**الدليل من قاعدة البيانات:**
```sql
-- Migration: 20260204072414_create_operations_maintenance_system.sql
CREATE FUNCTION calculate_cost_per_tree()
RETURNS TRIGGER AS $$
BEGIN
  SELECT total_trees INTO farm_total_trees
  FROM farms WHERE id = NEW.farm_id;

  NEW.cost_per_tree := NEW.total_amount / farm_total_trees;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trigger_calculate_cost_per_tree
  BEFORE INSERT OR UPDATE ON maintenance_fees
  FOR EACH ROW
  EXECUTE FUNCTION calculate_cost_per_tree();
```

---

### 8️⃣ هل يمكن تجميع أكثر من صيانة في رسوم واحدة؟

**⬜ لا**

**التوضيح:**
- حالياً، كل سجل صيانة يمكن أن يحتوي على **رسوم واحدة فقط**
- العلاقة: `maintenance_fees.maintenance_id` هو foreign key إلى `maintenance_records`
- **لا يوجد** آلية لتجميع عدة صيانات في رسوم واحدة

**الدليل:**
```sql
CREATE TABLE maintenance_fees (
  id uuid PRIMARY KEY,
  maintenance_id uuid NOT NULL REFERENCES maintenance_records(id),
  -- علاقة 1:1 (صيانة واحدة = رسوم واحدة)
  ...
);
```

---

### 9️⃣ هل يوجد في لوحة الإدارة متابعة السداد الخاصة برسوم الصيانة؟

**✅ موجود**

**التوضيح:**
يوجد نظام كامل لتتبع السدادات في جدول `maintenance_payments`:
- ✅ عدد المسددين/غير المسددين (payment_status)
- ✅ إجمالي المحصل والمتبقي (amount_paid, amount_due)
- ✅ تفاصيل كل عميل (user_id, tree_count)

**الدليل من قاعدة البيانات:**
```sql
-- Migration: 20260204073542_create_maintenance_payments_tracking.sql
CREATE TABLE maintenance_payments (
  id uuid PRIMARY KEY,
  user_id uuid NOT NULL,
  maintenance_fee_id uuid NOT NULL,
  tree_count int NOT NULL,
  amount_due numeric(10, 2) NOT NULL,
  amount_paid numeric(10, 2) DEFAULT 0,
  payment_status text DEFAULT 'pending' CHECK (payment_status IN ('pending', 'paid')),
  ...
);

CREATE VIEW maintenance_payments_summary AS
SELECT
  up.full_name,
  mp.tree_count,
  mp.amount_due,
  mp.amount_paid,
  mp.payment_status,
  ...
FROM maintenance_payments mp;
```

**لكن:**
- ❌ **لم يتم تنفيذ واجهة إدارية** لعرض هذه البيانات في `OperationsSection.tsx`
- الجداول موجودة في قاعدة البيانات فقط
- لا يوجد UI لعرض الملخص

---

### 🔟 هل تبويب أشجاري الخضراء ينتج بيانات جاهزة للاستهلاك في الواجهة؟

**✅ نعم**

**التوضيح:**
البيانات جاهزة تماماً للاستهلاك:
- ✅ **صيانة منشورة** (status = 'published')
- ✅ **مرفقات** (maintenance_media)
- ✅ **رسوم** (maintenance_fees)
- ✅ **مراحل** (maintenance_stages)

**View جاهز في قاعدة البيانات:**
```sql
CREATE VIEW maintenance_full_details AS
SELECT
  mr.id,
  f.name_ar as farm_name,
  mr.maintenance_type,
  mr.maintenance_date,
  mr.status,
  mf.total_amount,
  mf.cost_per_tree,
  COUNT(DISTINCT ms.id) as stages_count,
  COUNT(DISTINCT mm.id) as media_count
FROM maintenance_records mr
JOIN farms f ON f.id = mr.farm_id
LEFT JOIN maintenance_fees mf ON mf.maintenance_id = mr.id
LEFT JOIN maintenance_stages ms ON ms.maintenance_id = mr.id
LEFT JOIN maintenance_media mm ON mm.maintenance_id = mr.id;
```

**وظيفة لجلب بيانات العميل:**
```sql
CREATE FUNCTION get_client_maintenance_records(client_user_id uuid)
-- تُرجع جميع الصيانات الخاصة بمزارع العميل
-- مع حساب المستحقات تلقائياً
```

---

## ثانياً: التقييم الحالي

### ✅ ما تم تنفيذه بشكل صحيح:

1. **قاعدة البيانات كاملة ومتقنة:**
   - ✅ جداول منظمة (maintenance_records, stages, media, fees, payments)
   - ✅ حساب تلقائي لتكلفة الشجرة (trigger)
   - ✅ View جاهز لعرض البيانات الكاملة
   - ✅ RLS محكم على جميع الجداول
   - ✅ Foreign keys صحيحة مع CASCADE

2. **نموذج إدخال الصيانة:**
   - ✅ اختيار المزرعة
   - ✅ نوع الصيانة (دورية/موسمية/طارئة)
   - ✅ تاريخ التنفيذ
   - ✅ حالة (مسودة/منشور/مكتمل)

3. **إضافة مراحل متعددة:**
   - ✅ نموذج إضافة مرحلة
   - ✅ اسم + تعليق + تاريخ
   - ✅ عرض المراحل المضافة

4. **رفع ميديا:**
   - ✅ دعم صور وفيديو
   - ✅ Storage bucket منفصل
   - ✅ رفع وحذف

5. **رسوم الصيانة:**
   - ✅ إدخال مبلغ إجمالي
   - ✅ حساب تلقائي لتكلفة الشجرة
   - ✅ عرض التكلفة في الجدول

6. **العمل على مستوى المزرعة:**
   - ✅ النظام مرتبط بالمزارع فقط
   - ✅ لا يرتبط بالمستخدمين مباشرة
   - ✅ متوافق مع المواصفات

7. **البيانات جاهزة للواجهة:**
   - ✅ View كامل للبيانات
   - ✅ وظيفة لجلب صيانات العميل
   - ✅ حساب المستحقات تلقائياً

---

### ❌ ما لم يُنفذ:

1. **التسمية:**
   - ❌ لا يوجد تبويب باسم "أشجاري الخضراء" بشكل واضح
   - الموجود: قسم "التشغيل" بدون تصنيف فرعي

2. **تجميع الصيانات:**
   - ❌ لا يمكن تجميع أكثر من صيانة في رسوم واحدة
   - العلاقة حالياً: صيانة واحدة = رسوم واحدة

3. **واجهة متابعة السداد:**
   - ❌ لا يوجد UI في لوحة الإدارة لعرض:
     - عدد المسددين/غير المسددين
     - إجمالي المحصل والمتبقي
     - تفاصيل كل عميل
   - الجداول موجودة في قاعدة البيانات فقط بدون واجهة

4. **التبويبات داخل قسم التشغيل:**
   - ❌ لا يوجد تبويب فرعي "أشجاري الخضراء"
   - القسم حالياً عبارة عن نموذج واحد بدون تصنيفات

---

## ثالثاً: خطة التصحيح

### المرحلة 1: إعادة تنظيم قسم التشغيل

**الهدف:** إنشاء تبويبات منظمة داخل قسم التشغيل

**الخطوات:**
1. تحويل `OperationsSection.tsx` إلى مكون بتبويبات:
   - تبويب "أشجاري الخضراء" (الصيانة الزراعية)
   - تبويب "أشجاري الذهبية" (الصيانة الاستثمارية) - مستقبلاً
   - تبويب "التقارير"

2. نقل المحتوى الحالي إلى تبويب "أشجاري الخضراء"

---

### المرحلة 2: إضافة واجهة متابعة السداد

**الهدف:** عرض ملخص السدادات في لوحة الإدارة

**الخطوات:**
1. إنشاء مكون `MaintenancePaymentsManager.tsx`
2. عرض:
   - جدول بجميع السدادات
   - إحصائيات (مسدد/معلق)
   - فلترة حسب المزرعة/العميل
   - تصدير تقارير

---

### المرحلة 3: دعم تجميع الصيانات (اختياري)

**الهدف:** السماح بتجميع عدة صيانات في رسوم واحدة

**الخطوات:**
1. تعديل جدول `maintenance_fees`:
   - إضافة جدول وسيط `maintenance_fee_items`
   - علاقة many-to-many بين fees و records

2. تحديث الواجهة لدعم التجميع

---

### المرحلة 4: تحسين UX

**الهدف:** تحسين تجربة الاستخدام

**الخطوات:**
1. إضافة معاينة للصور والفيديوهات
2. إضافة تقدم رفع الملفات
3. إضافة تأكيدات أفضل
4. إضافة رسائل نجاح/خطأ واضحة

---

## رابعاً: التقييم النهائي

### الحالة الحالية:

**✅ قاعدة البيانات:** 95% مكتملة ومتقنة
**✅ نموذج الإدخال:** 90% مكتمل
**✅ المراحل:** 100% مكتملة
**✅ الميديا:** 100% مكتملة
**✅ الرسوم:** 85% مكتملة (ينقص واجهة متابعة السداد)
**⚠️ التبويبات:** 0% (لا يوجد تبويب "أشجاري الخضراء" منفصل)
**⚠️ تجميع الصيانات:** 0%

### النتيجة الإجمالية:

**التنفيذ الحالي:** 75% من المواصفات المطلوبة

**ما يعمل بشكل ممتاز:**
- قاعدة البيانات محكمة ومنظمة
- نموذج الإدخال يعمل بكفاءة
- الحسابات التلقائية تعمل
- البيانات جاهزة للواجهة

**ما يحتاج تصحيح:**
- إعادة تنظيم القسم بتبويبات واضحة
- إضافة واجهة متابعة السداد
- دعم تجميع الصيانات (اختياري)

---

## خامساً: الملاحظة الأخيرة

### ✅ التنفيذ الحالي **متوافق** مع المواصفات في:
- العمل على مستوى المزرعة
- إنتاج بيانات صيانة ورسوم جاهزة

### ⚠️ التنفيذ الحالي **يحتاج تصحيح** في:
- التسمية والتنظيم (تبويب "أشجاري الخضراء" غير واضح)
- واجهة متابعة السداد (موجودة في DB فقط)

---

**التوصية:**
تطبيق المرحلة 1 و 2 من خطة التصحيح لإكمال التنفيذ بشكل كامل حسب المواصفات المعتمدة.

**الأولوية:**
1. إعادة تنظيم القسم بتبويبات واضحة (عالية)
2. إضافة واجهة متابعة السداد (عالية)
3. دعم تجميع الصيانات (منخفضة - اختياري)
