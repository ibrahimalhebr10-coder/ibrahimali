# نظام دورات الاستثمار الذكية - أشجاري الذهبية

## التاريخ: 2026-02-04
## الحالة: ✅ مكتمل ومُختبر

---

## نظرة عامة

تم تطوير نظام **إداري ذكي** لـ"أشجاري الذهبية" داخل قسم التشغيل في لوحة الإدارة.

**موقع التطوير:**
```
لوحة الإدارة
  → قسم التشغيل
    → تبويب: أشجاري الذهبية
```

**❗ مهم:**
- ❌ لا واجهة عامة
- ❌ لا زر فوتر
- ❌ لا تجربة زائر
- ✅ التطوير إداري فقط

---

## الابتكار المحوري

### الدورة الاستثمارية الذكية

**المفهوم:**
```
سجل واحد فقط
  ↓
يمثل:
  • صيانة أشجار
  • استفادة من مخلفات
  • إدخال للمصنع
  • مزيج منهم في نفس الفترة
  ↓
رسوم واحدة توزّع على عدد الأشجار
  ↓
بدون تعقيد أو تكرار
```

**الفائدة:**
- للإدارة: إدخال واحد، قرار واحد، نشر واحد
- للمستثمر: قصة واضحة، رسوم مفهومة، لا تشويش
- للنظام: أقل تعقيد، أعلى ثبات، قابل للتوسع

---

## البنية التقنية

### 1. Database Schema

#### جدول `investment_cycles`

```sql
CREATE TABLE investment_cycles (
  id uuid PRIMARY KEY,
  farm_id uuid REFERENCES farms(id) ON DELETE CASCADE NOT NULL,
  cycle_types text[] NOT NULL DEFAULT '{}',
  cycle_date date NOT NULL DEFAULT CURRENT_DATE,
  description text NOT NULL,
  total_amount numeric(10, 2) DEFAULT 0,
  cost_per_tree numeric(10, 2) DEFAULT 0,  -- محسوبة تلقائياً
  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'published')),
  images text[] DEFAULT '{}',
  videos text[] DEFAULT '{}',
  visible_to_client boolean DEFAULT true,
  created_by uuid REFERENCES auth.users(id) NOT NULL,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

#### الحسابات التلقائية

```sql
-- تحسب تلقائياً عند الإدخال أو التعديل
cost_per_tree = total_amount ÷ farm.total_trees

-- مثال:
-- مزرعة بها 50 شجرة
-- إجمالي الرسوم: 5000 ريال
-- تكلفة الشجرة: 100 ريال (تلقائي)
```

#### Functions

##### 1. Readiness Check
```sql
check_investment_cycle_readiness(cycle_id uuid)
RETURNS jsonb
```

يفحص:
- ✓ مزرعة محددة
- ✓ وصف مكتوب
- ✓ نوع دورة محدد
- ✓ توثيق مرفوع
- ✓ رسوم مدخلة

##### 2. Payment Summary
```sql
get_investment_cycle_payment_summary(cycle_id uuid)
RETURNS jsonb
```

يعيد:
- عدد المستثمرين الكلي
- عدد المسددين
- عدد غير المسددين
- المبلغ المحصل
- المبلغ المتبقي

---

### 2. Service Layer

#### `investmentCyclesService.ts`

```typescript
interface InvestmentCycle {
  id: string;
  farm_id: string;
  cycle_types: string[];
  cycle_date: string;
  description: string;
  total_amount: number;
  cost_per_tree: number;
  status: 'draft' | 'published';
  images: string[];
  videos: string[];
  visible_to_client: boolean;
  created_by: string;
  created_at: string;
  updated_at: string;
}

interface InvestmentCycleReadiness {
  has_farm: boolean;
  has_description: boolean;
  has_cycle_types: boolean;
  has_documentation: boolean;
  has_cost: boolean;
  ready: boolean;
}

interface InvestmentCyclePaymentSummary {
  total_investors: number;
  paid_investors: number;
  pending_investors: number;
  total_collected: number;
  total_pending: number;
  cost_per_tree: number;
}
```

**Methods:**
- `getAllCycles()` - جميع الدورات
- `getCycleById(id)` - دورة واحدة
- `getCyclesByFarm(farmId)` - دورات مزرعة
- `createCycle(cycle)` - إنشاء دورة
- `updateCycle(id, updates)` - تحديث دورة
- `deleteCycle(id)` - حذف دورة
- `publishCycle(id)` - نشر دورة
- `unpublishCycle(id)` - إخفاء دورة
- `checkReadiness(cycleId)` - فحص الجاهزية
- `getPaymentSummary(cycleId)` - ملخص المدفوعات
- `uploadImage(file, cycleId)` - رفع صورة
- `uploadVideo(file, cycleId)` - رفع فيديو

---

### 3. UI Components

#### A. InvestmentCycleWizard
**الملف:** `src/components/admin/InvestmentCycleWizard.tsx`

**الوظائف:**
1. نموذج إدخال واحد مبسّط
2. مؤشر جاهزية تفاعلي
3. رفع توثيق (صور/فيديو)
4. حساب تلقائي لتكلفة الشجرة
5. منع النشر قبل الاكتمال

**الحقول:**

```typescript
{
  farm_id: string,           // اختيار المزرعة
  cycle_types: string[],     // نوع الدورة (متعدد)
  cycle_date: string,        // التاريخ
  description: string,       // وصف إجباري
  total_amount: number,      // المبلغ الإجمالي
  images: string[],          // الصور
  videos: string[],          // الفيديو
  status: 'draft' | 'published'
}
```

**أنواع الدورات:**
```
☑ صيانة أشجار (maintenance)
☑ مخلفات مزرعة (waste)
☑ مصنع: تمر/زيت (factory)
```

**مؤشر الجاهزية:**
```
┌─────────────────────────────────┐
│ ⚠ يحتاج استكمال                │
├─────────────────────────────────┤
│ ✓ المزرعة                       │
│ ✓ الوصف                         │
│ ✓ نوع الدورة                    │
│ ○ التوثيق                       │
│ ✓ الرسوم                        │
└─────────────────────────────────┘
```

عند الاكتمال:
```
┌─────────────────────────────────┐
│ ✓ جاهز للنشر                   │
├─────────────────────────────────┤
│ ✓ المزرعة                       │
│ ✓ الوصف                         │
│ ✓ نوع الدورة                    │
│ ✓ التوثيق                       │
│ ✓ الرسوم                        │
└─────────────────────────────────┘

[جاهز للنشر] ← زر يظهر فقط عند الاكتمال
```

**الحساب التلقائي:**
```
┌─────────────────────────────────┐
│ المبلغ الإجمالي: 5000 ريال      │
│                                 │
│ 📊 تكلفة الشجرة: 100.00 ريال   │
│ (المبلغ ÷ 50 شجرة)              │
└─────────────────────────────────┘
```

---

#### B. GoldenTreesInvestmentTab
**الملف:** `src/components/admin/GoldenTreesInvestmentTab.tsx`

**الوظائف:**
1. عرض جميع الدورات
2. حالة كل دورة (مسودة/منشور)
3. ملخص مدفوعات لكل دورة
4. إجراءات: تعديل، نشر/إخفاء، حذف

**عرض الدورة:**
```
┌─────────────────────────────────────────────┐
│ مزرعة النخيل الشمالية         [منشور]      │
│ دورة صيانة شاملة وتحسين جودة المنتج        │
│ 🕐 2024-01-15  │  صيانة + مصنع             │
├─────────────────────────────────────────────┤
│ 💰 المبلغ الإجمالي  │  📈 تكلفة الشجرة    │
│    5,000 ريال        │     100.00 ريال     │
│                                             │
│ ✓ مسددين: 35        │  ⚠ غير مسددين: 15  │
├─────────────────────────────────────────────┤
│ [عرض ملخص المدفوعات]                       │
└─────────────────────────────────────────────┘
```

**ملخص المدفوعات (عند التوسيع):**
```
┌─────────────────────────────────────────────┐
│ إجمالي المستثمرين    │  المبلغ المحصل      │
│        50             │   3,500 ريال        │
│                                             │
│ المبلغ المتبقي       │  نسبة التحصيل       │
│   1,500 ريال         │      70%            │
└─────────────────────────────────────────────┘
```

**الإجراءات:**
- ✏️ تعديل
- 👁️ نشر / إخفاء
- 🗑️ حذف

---

#### C. OperationsSection Integration
**الملف:** `src/components/admin/OperationsSection.tsx`

**التبويبات:**
```
┌──────────────────────────────────────────┐
│ [🌿 أشجاري الخضراء] [📈 أشجاري الذهبية] [💳 متابعة السداد] │
└──────────────────────────────────────────┘
      ↑ الصيانة          ↑ الاستثمار         ↑ المدفوعات
```

**الألوان:**
- الخضراء: `green-50`, `green-600`
- الذهبية: `amber-50`, `amber-600`
- السداد: `blue-50`, `blue-600`

---

## User Flow

### رحلة الإداري

```
1. دخول قسم التشغيل
   ↓
2. اختيار تبويب "أشجاري الذهبية"
   ↓
3. [إضافة دورة استثمار]
   ↓
4. نموذج الإدخال المبسّط
   ├─ اختيار المزرعة
   ├─ اختيار أنواع الدورة
   ├─ كتابة وصف مختصر
   ├─ إدخال المبلغ الإجمالي
   │  └─ يحسب تلقائياً: تكلفة الشجرة
   ├─ رفع صور (توثيق)
   └─ رفع فيديو (اختياري)
   ↓
5. فحص مؤشر الجاهزية
   ├─ ناقص؟ → استكمال
   └─ كامل؟ → [حفظ]
   ↓
6. بعد الحفظ: مؤشر الجاهزية تحديث
   ├─ كامل → زر [جاهز للنشر]
   └─ نشر
   ↓
7. عرض في القائمة
   ├─ عرض البيانات الأساسية
   ├─ [عرض ملخص المدفوعات]
   │  └─ عدد المسددين/غير المسددين
   │  └─ المبالغ المحصلة/المتبقية
   └─ إجراءات: تعديل، إخفاء، حذف
```

---

## الفرق بين الخضراء والذهبية

```
┌────────────────┬────────────────────┬────────────────────┐
│    العنصر      │    الخضراء         │    الذهبية         │
├────────────────┼────────────────────┼────────────────────┤
│ الطبيعة        │ صيانة تشغيلية     │ دورة استثمار       │
│ التكرار        │ سجلات متعددة      │ سجل واحد ذكي       │
│ الأنواع        │ منفصلة            │ مجمّعة (متعدد)     │
│ المراحل        │ موجودة            │ لا مراحل           │
│ التركيز        │ التنفيذ           │ القيمة             │
│ الجمهور        │ زراعي             │ استثماري          │
│ اللغة          │ يومية             │ استثمارية         │
└────────────────┴────────────────────┴────────────────────┘
```

---

## الممنوعات الصريحة

```
❌ حساب أرباح
❌ توقعات عوائد
❌ تفاصيل محاسبية
❌ خلط مع أشجاري الخضراء
❌ أكثر من نموذج إدخال
❌ مراحل أو نماذج فرعية
❌ جداول فردية للمستثمرين
```

---

## Security & RLS

### الصلاحيات

#### الإداريون
```
✅ إنشاء دورات
✅ قراءة جميع الدورات
✅ تعديل جميع الدورات
✅ حذف جميع الدورات
✅ نشر/إخفاء
```

#### المستثمرون
```
✅ قراءة الدورات المنشورة فقط
✅ للمزارع التي يملكون فيها أصول
❌ لا يمكنهم التعديل
❌ لا يمكنهم الحذف
❌ لا يمكنهم رؤية المسودات
```

### RLS Policies

```sql
-- الإداريون: كل الصلاحيات
CREATE POLICY "Admins can manage investment cycles"
  ON investment_cycles
  FOR ALL
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM admins
      WHERE admins.user_id = auth.uid()
    )
  );

-- المستثمرون: قراءة المنشور فقط
CREATE POLICY "Investors can view published cycles for their farms"
  ON investment_cycles
  FOR SELECT
  TO authenticated
  USING (
    status = 'published'
    AND visible_to_client = true
    AND EXISTS (
      SELECT 1 FROM reservations r
      WHERE r.farm_id = investment_cycles.farm_id
      AND r.user_id = auth.uid()
      AND r.status = 'active'
      AND r.path_type = 'investment'
    )
  );
```

---

## الملفات المُنشأة والمُعدّلة

### ✅ جديد

1. **Migration:**
   - `supabase/migrations/20260204140000_create_investment_cycles_system.sql`

2. **Service:**
   - `src/services/investmentCyclesService.ts`

3. **Components:**
   - `src/components/admin/InvestmentCycleWizard.tsx`
   - `src/components/admin/GoldenTreesInvestmentTab.tsx`

### ✅ محدّث

1. **Operations:**
   - `src/components/admin/OperationsSection.tsx`
     - إضافة تبويب "أشجاري الذهبية"
     - تكامل مع `GoldenTreesInvestmentTab`

---

## Testing Guide

### الاختبار 1: إنشاء دورة

```
1. دخول الإدارة
2. قسم التشغيل → أشجاري الذهبية
3. [إضافة دورة استثمار]
4. اختيار مزرعة
5. اختيار أنواع: ☑ صيانة + ☑ مصنع
6. كتابة وصف
7. إدخال مبلغ: 5000
   ✓ يحسب تلقائياً: 100 ريال/شجرة
8. رفع صور
9. فحص مؤشر الجاهزية
   ✓ يجب أن يكون كامل
10. [حفظ]
    ✓ نجح الحفظ
11. [جاهز للنشر]
    ✓ تحول إلى "منشور"
```

### الاختبار 2: ملخص المدفوعات

```
1. فتح دورة منشورة
2. [عرض ملخص المدفوعات]
   ✓ يظهر:
     - إجمالي المستثمرين
     - عدد المسددين
     - عدد غير المسددين
     - المبلغ المحصل
     - المبلغ المتبقي
     - نسبة التحصيل
```

### الاختبار 3: التعديل

```
1. اختيار دورة
2. [تعديل]
3. تغيير البيانات
4. [حفظ]
   ✓ تحديث البيانات
   ✓ تحديث cost_per_tree تلقائياً
```

### الاختبار 4: الإخفاء/النشر

```
1. دورة منشورة
2. [إخفاء]
   ✓ تحول إلى "مسودة"
3. [نشر] (بعد فحص الجاهزية)
   ✓ تحول إلى "منشور"
```

---

## الأمثلة العملية

### مثال 1: صيانة شاملة

```
المزرعة: مزرعة النخيل الشمالية (50 شجرة)
النوع: ☑ صيانة أشجار
التاريخ: 2024-02-15
الوصف: "صيانة شاملة تشمل التقليم والتسميد ومكافحة الآفات"
المبلغ: 5,000 ريال

← تكلفة الشجرة: 100 ريال (تلقائي)
```

### مثال 2: دورة مزدوجة

```
المزرعة: مزرعة التمور المركزية (100 شجرة)
النوع: ☑ صيانة + ☑ مصنع
التاريخ: 2024-03-01
الوصف: "صيانة دورية مع تحويل المحصول إلى تمور مجففة"
المبلغ: 8,000 ريال

← تكلفة الشجرة: 80 ريال (تلقائي)
```

### مثال 3: دورة كاملة

```
المزرعة: مزرعة الزيتون الجنوبية (75 شجرة)
النوع: ☑ صيانة + ☑ مخلفات + ☑ مصنع
التاريخ: 2024-04-10
الوصف: "دورة كاملة: صيانة، استفادة من المخلفات، وإنتاج زيت الزيتون"
المبلغ: 12,000 ريال

← تكلفة الشجرة: 160 ريال (تلقائي)
```

---

## التوسعات المستقبلية

### Phase 2: تحليلات متقدمة
- [ ] Dashboards تفاعلية
- [ ] مقارنة بين الدورات
- [ ] تقارير أداء
- [ ] تنبؤات التكاليف

### Phase 3: أتمتة
- [ ] جدولة دورات تلقائية
- [ ] إشعارات للمستثمرين
- [ ] ربط مع أنظمة الدفع
- [ ] سداد تلقائي

### Phase 4: تكامل
- [ ] ربط مع المصانع
- [ ] تتبع المنتجات
- [ ] إدارة المخزون
- [ ] تحليل الربحية

---

## الخلاصة

تم تطوير نظام **دورات الاستثمار الذكية** داخل قسم التشغيل:

✅ **سجل واحد ذكي** - بدل عدة نماذج معقدة
✅ **توزيع تلقائي** - الرسوم تُحسب على الأشجار
✅ **مؤشر جاهزية** - يمنع الأخطاء قبل النشر
✅ **ملخص مدفوعات** - متابعة مباشرة بدون تعقيد
✅ **UI بسيط وواضح** - تجربة سلسة للإداري
✅ **Security محكم** - RLS يحمي البيانات
✅ **Database ذكية** - حسابات تلقائية

**النظام جاهز للاستخدام!** ✅

البناء نجح بدون أخطاء:
```
✓ 1577 modules transformed.
✓ built in 9.16s
```

---

## المراجع

### الوثائق ذات الصلة
- `OPERATIONS_COMPLETE_SUMMARY.md` - قسم التشغيل الكامل
- `OPERATIONS_GREEN_TREES_TAB_CORRECTION_COMPLETE.md` - أشجاري الخضراء
- `GOLDEN_TREES_LUXURY_EXPERIENCE_IMPLEMENTATION.md` - التجربة الاستثمارية

### Code Patterns
- Smart Single Record Pattern
- Auto-calculation Pattern
- Readiness Indicator Pattern
- Payment Summary Pattern
