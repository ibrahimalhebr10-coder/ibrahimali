# تمييز العقود حسب المسار - التحديث الكامل
## Contract Types Path-Based Distinction - Complete Update

> **التاريخ:** 5 فبراير 2026
> **الحالة:** ✅ مُكتمل ومُختَبَر

---

## 🎯 الهدف من التحديث

تمييز واضح بين عقود المسارين في صفحة "حسابي" وربطها بشكل صحيح مع أزرار "أشجاري":

1. ✅ **عقد أشجاري الخضراء** - للمسار الزراعي (Agricultural)
2. ✅ **عقد أشجاري الذهبية** - للمسار الاستثماري (Investment)

---

## 🔴 المشكلة السابقة

### في صفحة "العقود" (MyContracts):

```typescript
// ❌ المشكلة القديمة
const getContractTypeBadge = (contractName: string | null) => {
  return { label: 'عقد زراعي' }; // ❌ دائماً نفس النص!
};
```

**النتائج السلبية:**
- ❌ جميع العقود تظهر باسم "عقد زراعي" بغض النظر عن المسار
- ❌ لا يوجد تمييز واضح بين المسارين
- ❌ المستثمر يرى "عقد زراعي" بدلاً من "عقد أشجاري الذهبية"
- ❌ لا يوجد ربط واضح بين العقد وزر "أشجاري"

### مثال:

```
المستثمر يحجز من مسار "أشجاري الذهبية"
    ↓
العقد يظهر في "حسابي"
    ↓
❌ مكتوب: "عقد زراعي" (خطأ!)
❌ لون أخضر (خطأ!)
❌ ليس واضح أنه من مسار الاستثمار
```

---

## ✅ الحل المُطبَّق

### 1️⃣ تحديث Interface Contract

**إضافة حقل `path_type`:**

```typescript
// ✅ الحل الجديد
interface Contract {
  id: string;
  farm_id: string;
  farm_name: string;
  // ... باقي الحقول
  path_type: 'agricultural' | 'investment'; // ✅ الحقل الجديد
}
```

**المصدر:** `src/components/MyContracts.tsx` - السطر 26-38

---

### 2️⃣ قراءة path_type من قاعدة البيانات

**تحديث كود جلب العقود:**

```typescript
// ✅ قراءة path_type من reservations
farmData.contracts.push({
  id: reservation.id,
  farm_id: farmId,
  farm_name: farmName,
  // ... باقي الحقول
  path_type: reservation.path_type || 'agricultural' // ✅ قراءة المسار
});
```

**المصدر:** `src/components/MyContracts.tsx` - السطر 92-105

**ملاحظة:** جدول `reservations` يحتوي على عمود `path_type` تم إضافته في Migration:
- **الملف:** `supabase/migrations/20260203195400_add_path_type_to_reservations_tree_pool_architecture.sql`
- **القيم المسموحة:** `'agricultural'` أو `'investment'`

---

### 3️⃣ تحديث دالة getContractTypeBadge

**قبل التحديث:**

```typescript
// ❌ القديم - يتجاهل المسار
const getContractTypeBadge = (contractName: string | null) => {
  if (!contractName) {
    return { label: 'عقد زراعي', color: 'from-green-500 to-emerald-600' };
  }
  if (contractName.toLowerCase().includes('investment')) {
    return { label: 'عقد استثماري', color: 'from-amber-500 to-yellow-600' };
  }
  return { label: 'عقد زراعي', color: 'from-green-500 to-emerald-600' };
};
```

**بعد التحديث:**

```typescript
// ✅ الجديد - يستخدم path_type
const getContractTypeBadge = (pathType: 'agricultural' | 'investment') => {
  if (pathType === 'investment') {
    return {
      label: 'عقد أشجاري الذهبية',  // ✅ اسم واضح
      color: 'from-amber-500 to-yellow-600',
      icon: TrendingUp  // ✅ أيقونة الاستثمار
    };
  }
  return {
    label: 'عقد أشجاري الخضراء',  // ✅ اسم واضح
    color: 'from-green-500 to-emerald-600',
    icon: Sparkles  // ✅ أيقونة الزراعة
  };
};
```

**المصدر:** `src/components/MyContracts.tsx` - السطر 136-148

**التحسينات:**
- ✅ يستخدم `path_type` بدلاً من `contract_name`
- ✅ نصوص واضحة ومميزة لكل مسار
- ✅ ألوان مناسبة لكل مسار
- ✅ أيقونات مختلفة لكل مسار

---

### 4️⃣ تحديث استدعاء getContractTypeBadge

**قبل:**

```typescript
// ❌ القديم - يمرر contract_name
const badge = getContractTypeBadge(contract.contract_name);
const isAgriculturalContract = badge.label === 'عقد زراعي';
```

**بعد:**

```typescript
// ✅ الجديد - يمرر path_type
const badge = getContractTypeBadge(contract.path_type);
const ContractIcon = badge.icon;  // ✅ أيقونة مخصصة
const isAgriculturalContract = contract.path_type === 'agricultural';
```

**المصدر:** `src/components/MyContracts.tsx` - السطر 224-229

---

### 5️⃣ تحديث أيقونة العقد

**قبل:**

```tsx
{/* ❌ القديم - نفس الأيقونة للجميع */}
<Scroll className="w-8 h-8 text-white" />
```

**بعد:**

```tsx
{/* ✅ الجديد - أيقونة مخصصة حسب المسار */}
<ContractIcon className="w-8 h-8 text-white" />
{/* ContractIcon = Sparkles للزراعي، TrendingUp للاستثماري */}
```

**المصدر:** `src/components/MyContracts.tsx` - السطر 260

---

## 🔗 الربط مع أزرار "أشجاري"

### تدفق الربط الكامل:

```
1. المستخدم يحجز من أحد المسارين:
   - المسار الزراعي (أشجاري الخضراء) → path_type = 'agricultural'
   - المسار الاستثماري (أشجاري الذهبية) → path_type = 'investment'
        ↓
2. يُحفظ في قاعدة البيانات:
   - جدول: reservations
   - عمود: path_type
   - القيمة: 'agricultural' أو 'investment'
        ↓
3. يظهر في صفحة "حسابي":
   - عقد أشجاري الخضراء (أخضر، Sparkles) ← إذا كان path_type = 'agricultural'
   - عقد أشجاري الذهبية (ذهبي، TrendingUp) ← إذا كان path_type = 'investment'
        ↓
4. عند الضغط على زر "أشجاري" في الفوتر:
   - يفتح MyGreenTrees مع identity صحيحة
   - يعرض الصيانات والمعلومات حسب المسار
        ↓
5. التناسق الكامل:
   ✅ لون العقد = لون الزر
   ✅ اسم العقد = اسم الزر
   ✅ أيقونة العقد = أيقونة المسار
   ✅ البيانات = حسب path_type
```

---

## 🎨 التصميم المرئي

### عقد أشجاري الخضراء (Agricultural):

```
╔═══════════════════════════════════════════╗
║  [🌟 Sparkles]                            ║
║  عقد أشجاري الخضراء                      ║
║                                           ║
║  🌳 50 شجرة                               ║
║  🗓️ المدة: 5 سنوات                       ║
║  📍 مزرعة الواحة                          ║
║                                           ║
║  [Gradient: أخضر زمردي]                  ║
╚═══════════════════════════════════════════╝
```

### عقد أشجاري الذهبية (Investment):

```
╔═══════════════════════════════════════════╗
║  [📈 TrendingUp]                          ║
║  عقد أشجاري الذهبية                      ║
║                                           ║
║  🌟 100 شجرة                              ║
║  🗓️ المدة: 3 سنوات                       ║
║  📍 مزرعة النخيل                          ║
║                                           ║
║  [Gradient: ذهبي مشمس]                   ║
╚═══════════════════════════════════════════╝
```

---

## 📊 مقارنة قبل وبعد

| العنصر | قبل التحديث ❌ | بعد التحديث ✅ |
|--------|----------------|-----------------|
| **اسم العقد** | "عقد زراعي" دائماً | "عقد أشجاري الخضراء" أو "عقد أشجاري الذهبية" |
| **الأيقونة** | Scroll للجميع | Sparkles للزراعي، TrendingUp للاستثماري |
| **اللون** | أخضر دائماً | أخضر للزراعي، ذهبي للاستثماري |
| **المصدر** | contract_name (غير دقيق) | path_type (دقيق 100%) |
| **التمييز** | غير واضح | واضح تماماً |
| **الربط مع الأزرار** | ضعيف | قوي ومنطقي |

---

## 🔄 تدفق كامل: من الحجز إلى العرض

### سيناريو 1: مسار أشجاري الخضراء (Farmer)

```
1. المستخدم يختار مسار "أشجاري الخضراء"
2. يختار مزرعة "مزرعة الواحة"
3. يحجز 50 شجرة زيتون
4. يدفع ويتم تأكيد الحجز
        ↓
5. في قاعدة البيانات:
   {
     user_id: "user-123",
     farm_id: "farm-456",
     path_type: "agricultural",  ← المفتاح!
     total_trees: 50,
     tree_types: "زيتون"
   }
        ↓
6. في صفحة "حسابي":
   - العنوان: "عقد أشجاري الخضراء"
   - الأيقونة: ✨ (Sparkles)
   - اللون: أخضر زمردي
        ↓
7. عند الضغط على زر "أشجاري الخضراء" في الفوتر:
   - يفتح MyGreenTrees
   - identity = 'agricultural'
   - يعرض الصيانات الزراعية
   - يعرض رسوم الصيانة للـ 50 شجرة
```

### سيناريو 2: مسار أشجاري الذهبية (Investor)

```
1. المستخدم يختار مسار "أشجاري الذهبية"
2. يختار مزرعة "مزرعة النخيل"
3. يحجز 100 شجرة نخيل
4. يدفع ويتم تأكيد الحجز
        ↓
5. في قاعدة البيانات:
   {
     user_id: "user-789",
     farm_id: "farm-012",
     path_type: "investment",  ← المفتاح!
     total_trees: 100,
     tree_types: "نخيل"
   }
        ↓
6. في صفحة "حسابي":
   - العنوان: "عقد أشجاري الذهبية"
   - الأيقونة: 📈 (TrendingUp)
   - اللون: ذهبي مشمس
        ↓
7. عند الضغط على زر "أشجاري الذهبية" في الفوتر:
   - يفتح MyGreenTrees
   - identity = 'investment'
   - يعرض الصيانات الاستثمارية
   - يعرض رسوم الصيانة للـ 100 شجرة
```

---

## 🧪 سيناريوهات الاختبار

### الاختبار 1: حجز من مسار أشجاري الخضراء

```bash
1. سجل دخول كمزارع
2. اختر مسار "أشجاري الخضراء"
3. احجز أشجار من أي مزرعة
4. أكمل الدفع
5. اذهب إلى "حسابي"

النتيجة المتوقعة:
✅ يظهر عقد بعنوان: "عقد أشجاري الخضراء"
✅ الأيقونة: ✨ (Sparkles)
✅ اللون: أخضر زمردي
✅ زر "أشجاري الخضراء" في الفوتر يعرض معلومات صحيحة
```

### الاختبار 2: حجز من مسار أشجاري الذهبية

```bash
1. سجل دخول كمستثمر
2. اختر مسار "أشجاري الذهبية"
3. احجز أشجار من أي مزرعة
4. أكمل الدفع
5. اذهب إلى "حسابي"

النتيجة المتوقعة:
✅ يظهر عقد بعنوان: "عقد أشجاري الذهبية"
✅ الأيقونة: 📈 (TrendingUp)
✅ اللون: ذهبي مشمس
✅ زر "أشجاري الذهبية" في الفوتر يعرض معلومات صحيحة
```

### الاختبار 3: عقود متعددة من مسارين مختلفين

```bash
1. سجل دخول
2. احجز من مسار "أشجاري الخضراء"
3. غيّر الهوية إلى مستثمر
4. احجز من مسار "أشجاري الذهبية"
5. اذهب إلى "حسابي"

النتيجة المتوقعة:
✅ يظهر عقدين مختلفين:
   - عقد أشجاري الخضراء (أخضر، Sparkles)
   - عقد أشجاري الذهبية (ذهبي، TrendingUp)
✅ كل عقد مرتبط بمساره الصحيح
✅ عند تبديل الهوية، زر "أشجاري" يتغير ليناسب
```

### الاختبار 4: التناسق بين العقد والزر

```bash
1. احجز من مسار "أشجاري الخضراء"
2. اذهب إلى "حسابي"
3. لاحظ لون وأيقونة العقد
4. اضغط على زر "أشجاري الخضراء" في الفوتر

النتيجة المتوقعة:
✅ لون العقد = لون الزر
✅ أيقونة العقد تتناسب مع المسار
✅ البيانات المعروضة تتطابق مع path_type
✅ لا يوجد تضارب أو تشويش
```

---

## 📁 الملفات المُعدَّلة

### 1. `src/components/MyContracts.tsx` ✅

**التغييرات:**

1. **السطر 26-38:** إضافة `path_type` إلى interface Contract
2. **السطر 92-105:** قراءة `path_type` من reservations
3. **السطر 136-148:** تحديث `getContractTypeBadge` لاستخدام `path_type`
4. **السطر 224-229:** تحديث استدعاء `getContractTypeBadge`
5. **السطر 260:** استخدام `ContractIcon` المخصصة

**عدد الأسطر المُعدَّلة:** 5 مواقع

---

## 🗄️ قاعدة البيانات

### جدول `reservations`:

```sql
-- العمود المستخدم
path_type TEXT NOT NULL DEFAULT 'agricultural'
CHECK (path_type IN ('agricultural', 'investment'))

-- تم إضافته في Migration:
-- 20260203195400_add_path_type_to_reservations_tree_pool_architecture.sql
```

**القيم المسموحة:**
- ✅ `'agricultural'` - مسار أشجاري الخضراء
- ✅ `'investment'` - مسار أشجاري الذهبية

**Index للأداء:**
```sql
CREATE INDEX idx_reservations_path_type ON reservations(path_type);
CREATE INDEX idx_reservations_user_path ON reservations(user_id, path_type);
```

---

## 🔍 الارتباطات بين المكونات

### المكونات المشاركة:

```
┌─────────────────────────────────────────────────────────┐
│                      App.tsx                            │
│  - يدير جميع الحالات                                   │
│  - يربط بين المكونات                                   │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ├─────────────────────────────────────┐
                      │                                     │
          ┌───────────▼───────────┐          ┌─────────────▼─────────────┐
          │  AccountProfile       │          │  MyGreenTrees             │
          │  - يعرض "حسابي"       │          │  - يعرض الصيانات          │
          │  - زر "أشجاري"        │◄─────────│  - حسب identity           │
          └───────────┬───────────┘          └───────────────────────────┘
                      │
          ┌───────────▼───────────┐
          │  MyContracts          │
          │  - يعرض العقود         │
          │  - يستخدم path_type    │
          │  - تمييز حسب المسار    │
          └───────────────────────┘
                      │
                      │
          ┌───────────▼───────────┐
          │  Database             │
          │  - reservations       │
          │  - path_type column   │
          └───────────────────────┘
```

---

## ✅ النتائج النهائية

### ما تم تحقيقه:

1. ✅ **تمييز واضح** - كل عقد يظهر باسم مساره الصحيح
2. ✅ **ألوان مناسبة** - أخضر للزراعي، ذهبي للاستثماري
3. ✅ **أيقونات مميزة** - Sparkles للزراعي، TrendingUp للاستثماري
4. ✅ **ربط قوي** - العقد مرتبط بزر "أشجاري" المناسب
5. ✅ **دقة البيانات** - استخدام `path_type` من قاعدة البيانات مباشرة
6. ✅ **تجربة متناسقة** - كل شيء يعمل معاً بانسجام

### Build Status:

```bash
npm run build
✓ built in 9.94s
✅ No errors
```

---

## 🎯 القاعدة الذهبية

```
العقد = المسار المُختار عند الحجز

path_type = 'agricultural' → عقد أشجاري الخضراء (🌿 أخضر)
path_type = 'investment'   → عقد أشجاري الذهبية (🌟 ذهبي)

لا استثناءات، لا تخمين، path_type هو مصدر الحقيقة!
```

---

## 🔮 ملاحظات هامة

### 1. التوافق مع العقود القديمة:

```typescript
// إذا لم يكن path_type موجود في عقد قديم:
path_type: reservation.path_type || 'agricultural'  // default زراعي
```

### 2. التناسق مع Identity System:

- ✅ `identity === 'agricultural'` → يعرض عقود أشجاري الخضراء بوضوح
- ✅ `identity === 'investment'` → يعرض عقود أشجاري الذهبية بوضوح

### 3. التناسق مع MyGreenTrees:

- ✅ عند فتح "أشجاري الخضراء" → يعرض صيانات path_type='agricultural'
- ✅ عند فتح "أشجاري الذهبية" → يعرض صيانات path_type='investment'

---

## 🎉 الخلاصة

**المشكلة:** عقود غير واضحة، كلها تظهر باسم "عقد زراعي"

**الحل:** استخدام `path_type` من قاعدة البيانات لتمييز دقيق

**النتيجة:**
- ✅ عقد أشجاري الخضراء (أخضر، Sparkles)
- ✅ عقد أشجاري الذهبية (ذهبي، TrendingUp)
- ✅ ربط واضح ومنطقي مع أزرار "أشجاري"

**الحالة:** ✅ مُكتمل بالكامل ومُختَبَر

---

**الآن العقود تعكس المسار الصحيح بدقة ووضوح تام!** 🎉
