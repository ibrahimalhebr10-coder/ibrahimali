# حساب المدير العام للتطوير

## معلومات الدخول الثابتة

تم إنشاء حساب مدير عام بمعلومات ثابتة لتسهيل عملية التطوير والاختبار.

---

## بيانات الدخول

### البريد الإلكتروني
```
admin@dev.com
```

### كلمة المرور
```
Admin@123
```

### الدور
```
super_admin (المدير العام)
```

---

## كيفية الدخول

### الطريقة الأولى: من زر الإدارة المرئي

1. افتح التطبيق في المتصفح
2. انظر للزاوية اليسرى من الهيدر
3. سترى منطقة زرقاء خفيفة جداً
4. اضغط عليها **4 مرات متتالية**
5. ستفتح صفحة تسجيل الدخول

### الطريقة الثانية: الوصول المباشر

يمكنك الوصول مباشرة عبر الرابط:
```
http://localhost:5173/#admin-login
```

---

## صفحة تسجيل الدخول

عند فتح صفحة تسجيل الدخول، ستجد:

### 1. معلومات الدخول معروضة بوضوح

في أسفل الصفحة، يوجد صندوق أزرق يحتوي على:
- البريد الإلكتروني: `admin@dev.com`
- كلمة المرور: `Admin@123`
- أزرار نسخ سريع لكل منهما

### 2. خطوات تسجيل الدخول

**الخطوة الأولى: إدخال البريد الإلكتروني**
- أدخل: `admin@dev.com`
- اضغط "متابعة"

**الخطوة الثانية: إدخال كلمة المرور**
- أدخل: `Admin@123`
- اضغط "دخول لوحة الإدارة"

### 3. الدخول الناجح

بعد إدخال المعلومات الصحيحة:
- سيتم تسجيل دخولك تلقائياً
- ستفتح لوحة الإدارة
- ستكون لديك **صلاحيات كاملة** كمدير عام

---

## الصلاحيات

كمدير عام، لديك الوصول الكامل إلى:

### ✅ إدارة المزارع
- عرض جميع المزارع
- إنشاء مزارع جديدة
- تعديل معلومات المزارع
- حذف المزارع
- إدارة الباقات الزراعية والاستثمارية

### ✅ إدارة الحجوزات
- عرض جميع الحجوزات
- الموافقة/رفض الحجوزات
- تحديث حالة الحجوزات
- إدارة دورة الحياة الكاملة للحجوزات

### ✅ إدارة المدفوعات
- عرض جميع المعاملات المالية
- التحقق من إيصالات السداد
- إدارة وسائل الدفع
- تفعيل/تعطيل بوابات الدفع

### ✅ إدارة المستخدمين
- عرض جميع المستثمرين
- إدارة ملفات المستخدمين
- إرسال رسائل للمستثمرين

### ✅ إدارة النظام
- إعدادات عامة للنظام
- إدارة القوالب
- إدارة القنوات
- إدارة المحتوى

### ✅ إدارة المديرين
- إضافة مديرين جدد
- تعيين الأدوار
- إدارة الصلاحيات
- تخصيص المزارع

---

## نطاق الصلاحيات

```
scope_type: all
scope_value: null
```

**معنى ذلك:**
- صلاحيات **غير محدودة** على جميع الأقسام
- الوصول إلى **جميع المزارع**
- لا توجد قيود على أي عملية

---

## التفاصيل التقنية

### معلومات قاعدة البيانات

**جدول auth.users:**
```sql
email: admin@dev.com
role: authenticated
raw_app_meta_data: {"provider":"email","providers":["email"],"role":"super_admin"}
raw_user_meta_data: {"full_name":"المدير العام"}
```

**جدول admins:**
```sql
email: admin@dev.com
full_name: المدير العام
role: super_admin
role_id: [UUID from admin_roles]
is_active: true
scope_type: all
scope_value: null
permissions: {
  "view_farms": true,
  "manage_farms": true,
  "view_reservations": true,
  "manage_reservations": true,
  "view_payments": true,
  "manage_payments": true,
  "view_analytics": true,
  "manage_users": true,
  "manage_settings": true,
  "manage_admins": true
}
```

---

## ملاحظات هامة

### للتطوير فقط

هذا الحساب مخصص **للتطوير والاختبار فقط**.

**يجب القيام بالآتي قبل النشر:**
1. ✅ حذف هذا الحساب من قاعدة البيانات
2. ✅ إزالة معلومات الدخول من صفحة AdminLogin
3. ✅ تغيير جميع كلمات المرور
4. ✅ إنشاء حسابات إنتاج آمنة

### الأمان

#### في التطوير (الحالي)
```
✅ معلومات واضحة وثابتة
✅ سهولة الوصول
✅ اختبار سريع
```

#### في الإنتاج (المطلوب)
```
❌ لا معلومات معروضة في الصفحة
❌ كلمات مرور قوية ومشفرة
❌ حسابات فردية لكل مدير
❌ تسجيل جميع الأنشطة
```

---

## الاختبار

### اختبار تسجيل الدخول

1. **فتح صفحة تسجيل الدخول:**
   - اضغط على المنطقة الزرقاء في الهيدر 4 مرات

2. **إدخال البريد:**
   - اكتب `admin@dev.com` أو انسخه من الصفحة
   - اضغط "متابعة"

3. **إدخال كلمة المرور:**
   - اكتب `Admin@123` أو انسخه من الصفحة
   - اضغط "دخول لوحة الإدارة"

4. **التحقق من النجاح:**
   - يجب أن تفتح لوحة الإدارة
   - يجب أن ترى اسم "المدير العام" في الواجهة
   - يجب أن تكون جميع القوائم متاحة

### اختبار الصلاحيات

1. **إدارة المزارع:**
   - انتقل إلى قسم "المزارع"
   - تأكد من القدرة على إضافة/تعديل/حذف

2. **إدارة الحجوزات:**
   - انتقل إلى قسم "الحجوزات"
   - تأكد من رؤية جميع الحجوزات
   - تأكد من القدرة على تحديث الحالات

3. **إدارة المدفوعات:**
   - انتقل إلى قسم "المدفوعات"
   - تأكد من رؤية جميع المعاملات
   - تأكد من القدرة على التحقق من الإيصالات

4. **إدارة المديرين:**
   - انتقل إلى قسم "المديرين"
   - تأكد من القدرة على إضافة مديرين جدد

---

## استكشاف الأخطاء

### مشكلة: لا يمكن الدخول

**الحلول:**
1. تأكد من إدخال البريد الإلكتروني بالضبط: `admin@dev.com`
2. تأكد من إدخال كلمة المرور بالضبط: `Admin@123`
3. تحقق من أن الحساب موجود في قاعدة البيانات:
   ```sql
   SELECT * FROM admins WHERE email = 'admin@dev.com';
   ```

### مشكلة: "هذا الحساب غير مخوّل"

**الحلول:**
1. تحقق من أن `is_active = true` في جدول admins
2. تحقق من أن الحساب موجود في auth.users
3. أعد تشغيل migration إنشاء الحساب

### مشكلة: لا أرى جميع الأقسام

**الحلول:**
1. تحقق من أن `scope_type = 'all'`
2. تحقق من permissions في جدول admins
3. تأكد من أن role_id يشير إلى super_admin

---

## إعادة إنشاء الحساب

إذا تم حذف الحساب أو حدثت مشكلة، يمكن إعادة إنشائه بتشغيل migration:

```sql
-- في Supabase SQL Editor أو Terminal

-- تشغيل ملف Migration
-- الملف: supabase/migrations/..._create_dev_super_admin_account.sql
```

**أو يمكنك تشغيل الكود مباشرة:**

```sql
DO $$
DECLARE
  v_user_id uuid;
  v_super_admin_role_id uuid;
BEGIN
  SELECT id INTO v_super_admin_role_id
  FROM admin_roles WHERE role_key = 'super_admin' LIMIT 1;

  DELETE FROM auth.users WHERE email = 'admin@dev.com';

  INSERT INTO auth.users (
    instance_id, id, aud, role, email, encrypted_password,
    email_confirmed_at, recovery_sent_at, last_sign_in_at,
    raw_app_meta_data, raw_user_meta_data,
    created_at, updated_at,
    confirmation_token, email_change, email_change_token_new, recovery_token
  ) VALUES (
    '00000000-0000-0000-0000-000000000000',
    gen_random_uuid(), 'authenticated', 'authenticated',
    'admin@dev.com', crypt('Admin@123', gen_salt('bf')),
    now(), now(), now(),
    '{"provider":"email","providers":["email"],"role":"super_admin"}',
    '{"full_name":"المدير العام"}',
    now(), now(), '', '', '', ''
  ) RETURNING id INTO v_user_id;

  DELETE FROM admins WHERE email = 'admin@dev.com';

  INSERT INTO admins (
    id, user_id, email, full_name, role, role_id,
    permissions, is_active, scope_type, scope_value,
    created_at, updated_at
  ) VALUES (
    gen_random_uuid(), v_user_id, 'admin@dev.com', 'المدير العام',
    'super_admin', v_super_admin_role_id,
    jsonb_build_object(
      'view_farms', true, 'manage_farms', true,
      'view_reservations', true, 'manage_reservations', true,
      'view_payments', true, 'manage_payments', true,
      'view_analytics', true, 'manage_users', true,
      'manage_settings', true, 'manage_admins', true
    ),
    true, 'all', null, now(), now()
  );
END $$;
```

---

## ملخص سريع

```
📧 البريد: admin@dev.com
🔐 كلمة المرور: Admin@123
🎯 الدور: super_admin
✨ الصلاحيات: كاملة (all)
🔓 الوصول: 4 ضغطات على المنطقة الزرقاء في الهيدر
```

---

## الخطوات التالية

### أثناء التطوير
1. ✅ استخدم هذا الحساب لاختبار جميع المميزات
2. ✅ أضف المزارع والحجوزات والمدفوعات
3. ✅ اختبر جميع الصلاحيات
4. ✅ تأكد من عمل RLS بشكل صحيح

### قبل النشر
1. ⚠️ احذف هذا الحساب من قاعدة البيانات
2. ⚠️ أزل معلومات الدخول من AdminLogin.tsx
3. ⚠️ أنشئ حسابات إنتاج آمنة
4. ⚠️ غيّر جميع كلمات المرور

---

**تاريخ الإنشاء:** 2026-02-02

**الحالة:** نشط ✅

**الاستخدام:** تطوير واختبار فقط ⚠️
