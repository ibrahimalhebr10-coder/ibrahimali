# أشجاري الخضراء - مرجع الترتيب السريع

## الترتيب الإلزامي

```
1. الحالة العامة
   ┌──────────────────────────────────┐
   │  💚 مطمئنة                       │
   │  أشجارك تمر بمرحلة عناية مستمرة │
   └──────────────────────────────────┘

2. الصيانة الأساسية (دائماً أولاً)
   ┌──────────────────────────────────┐
   │  🌿 قص                           │
   │  🌱 تسميد                        │
   │  💧 ري                           │
   └──────────────────────────────────┘

3. الصيانة الموسمية
   ┌──────────────────────────────────┐
   │  🛡️ مكافحة آفات                 │
   │  ✂️ تقليم موسمي                 │
   │  🌍 تحسين تربة                  │
   └──────────────────────────────────┘

4. التوثيق البصري
   (صور + فيديوهات داخل التفاصيل)

5. الرسوم
   (أسفل الصفحة)
```

---

## الكود الأساسي

### ترتيب الأولويات
```typescript
const priorities = {
  'pruning': 1,        // 🌿 قص
  'fertilization': 2,  // 🌱 تسميد
  'irrigation': 3,     // 💧 ري
  'pest_control': 4,   // 🛡️ مكافحة آفات
  'seasonal_pruning': 5,
  'soil_improvement': 6
};
```

### دالة الترتيب
```typescript
import { sortMaintenanceRecordsByPriority } from '../services/demoDataService';

// في loadData:
if (identity === 'agricultural') {
  records = sortMaintenanceRecordsByPriority(records);
}
```

### عرض Labels
```typescript
import { getMaintenanceTypeLabel } from '../services/demoDataService';

// في UI:
{identity === 'agricultural'
  ? getMaintenanceTypeLabel(type)
  : clientMaintenanceService.getMaintenanceTypeLabel(type)
}
```

---

## الشروط

### ✅ يجب
- قص + تسميد + ري دائماً أولاً
- نفس الترتيب في Demo والحقيقي
- الحالة العامة في الأعلى

### ❌ ممنوع
- صيانة موسمية قبل الأساسيات
- إخفاء الأنواع الأساسية
- تطبيق على المسار الذهبي

---

## الملفات المعدلة

1. `src/services/demoDataService.ts`
   - البيانات + دوال الترتيب

2. `src/components/MyGreenTrees.tsx`
   - العرض + الترتيب + الحالة العامة

---

## الاختبار السريع

```
1. افتح بدون تسجيل
2. اضغط "أشجاري الخضراء"
3. ✅ أول شيء: "مطمئنة"
4. ✅ أول سجل: قص
5. ✅ ثاني سجل: تسميد
6. ✅ ثالث سجل: ري
```

---

**الحالة: ✅ مكتمل ومُختبر**

للتفاصيل الكاملة: `GREEN_TREES_CONTENT_ORDER_IMPLEMENTATION.md`
