# دليل الاختبار التجريبي: معمارية خزان الأشجار

## الحالة: تم إنشاء حجوزات تجريبية ✅

تم إنشاء سيناريوهين تجريبيين كاملين لتوضيح كيفية عمل النظام.

---

## 📊 البيانات التجريبية المُنشأة

### المزرعة المستخدمة (الخزان):
- **الاسم:** مزرعة الزيتون المتطورة
- **ID:** `a910bce1-166b-4deb-aab4-26c5fe485e6d`
- **الأشجار المتاحة:** 400,000 شجرة
- **الحالة:** نشطة

> **ملاحظة:** نفس المزرعة تُستخدم للمسارين (زراعي واستثماري)

---

## 🌱 السيناريو الأول: المسار الزراعي

### معلومات الحجز:
```
معرف الحجز: 8f71596b-f1c0-4743-9252-2165a36a64de
المستخدم: مستخدم تجريبي - زراعي
البريد: 966544433244@phone.local
User ID: 1807dcc6-8306-4fe1-b30e-ca9c5fc28ebb

المزرعة: مزرعة الزيتون المتطورة
المسار: agricultural (زراعي)
عدد الأشجار: 50 شجرة
السعر الإجمالي: 25,000 ريال
الحالة: confirmed (مؤكد)
```

### ما حدث تلقائياً:
1. ✅ تم إنشاء حجز في جدول `reservations`
2. ✅ تم تسجيل `path_type = 'agricultural'`
3. ✅ الحجز جاهز للعرض في المسار الزراعي

### في قسم "مزرعتي" (لوحة التحكم):

#### المدير يفتح: التبويب الزراعي

**التبويبات المتاحة:**
1. **خريطة الأشجار**
   - تعرض 50 شجرة مسحوبة من الخزان
   - مصدرها: `reservations` حيث `path_type = 'agricultural'`

2. **العمليات الزراعية**
   - يسجل العمليات على هذه الأشجار
   - مثل: الري، التسميد، المكافحة

3. **التوثيق الزراعي**
   - يرفع صور لمراحل النمو
   - يوثق التطور الزراعي

4. **مراحل النمو**
   - يبني رحلة نمو الأشجار
   - من الشتلة إلى الإثمار

5. **Experience Builder**
   - يصيغ التجربة النهائية
   - يضيف محتوى تفاعلي

**ما لا يفعله المدير هنا:**
- ❌ لا يسحب أشجاراً من الخزان مباشرة
- ❌ لا ينشئ حجوزات جديدة
- ✅ فقط يدير التجربة للأشجار المسحوبة

---

### في الواجهة الأمامية:

المستخدم يسجل الدخول بالبريد: `966544433244@phone.local`

**زر "مزرعتي" في الفوتر:**

عند الضغط على الزر:
```javascript
// النظام يقرأ
const userReservations = await supabase
  .from('reservations')
  .select('*')
  .eq('user_id', userId)
  .eq('path_type', 'agricultural')  // ← المسار الزراعي
  .in('status', ['confirmed', 'paid', 'completed']);

// يعرض: 50 شجرة من مزرعة الزيتون المتطورة
```

**ما يراه المستخدم:**
- 🌳 عنوان: "محصولي الزراعي" أو "مزرعتي الزراعية"
- 📍 الموقع: مزرعة الزيتون المتطورة
- 🌱 عدد الأشجار: 50 شجرة
- 📸 الصور والمراحل (التي أضافها المدير)
- 📋 العمليات الزراعية (التي سجلها المدير)
- 🎯 التجربة التفاعلية (من Experience Builder)

**ملاحظة:**
- ✅ المستخدم لا يرى "المزرعة" مباشرة
- ✅ يرى فقط "رحلة أشجاره" المسحوبة من الخزان
- ✅ التجربة مصممة خصيصاً للمسار الزراعي

---

## 💼 السيناريو الثاني: المسار الاستثماري

### معلومات الحجز:
```
معرف الحجز: 447a479a-410c-4c69-9b93-0a35ec0b4859
المستخدم: مستخدم تجريبي - استثماري
البريد: 966544444444@phone.local
User ID: 03758ec2-bf7d-4873-8a74-d8a609f9e8fd

المزرعة: مزرعة الزيتون المتطورة  ← نفس المزرعة!
المسار: investment (استثماري)
عدد الأشجار: 75 شجرة
السعر الإجمالي: 45,000 ريال
الحالة: paid (مدفوع)
```

### ما حدث تلقائياً:
1. ✅ تم إنشاء حجز في جدول `reservations`
2. ✅ تم تسجيل `path_type = 'investment'`
3. ✅ **تلقائياً عبر Trigger:**
   - تم إنشاء سجل في `investment_agricultural_assets`
   - تم إنشاء حالة في `investment_status_tracking`
4. ✅ الأصول جاهزة للعرض في المسار الاستثماري

### التحقق من البناء التلقائي:

**جدول `investment_agricultural_assets`:**
```
Asset ID: ce8484b2-5c90-4050-9cd9-c2a6649185ad
User ID: 03758ec2-bf7d-4873-8a74-d8a609f9e8fd
Contract ID: 447a479a-410c-4c69-9b93-0a35ec0b4859  ← يشير للحجز
Farm ID: a910bce1-166b-4deb-aab4-26c5fe485e6d  ← من الخزان
نوع الشجرة: متنوع
الكمية: 75 شجرة
تاريخ الاستحواذ: 2026-02-03
ملاحظات: "تم الإنشاء تلقائياً من الحجز"
```

**جدول `investment_status_tracking`:**
```
Status ID: 99a719d6-a9b0-4a0e-8a79-7c3e775718e8
User ID: 03758ec2-bf7d-4873-8a74-d8a609f9e8fd
Contract ID: 447a479a-410c-4c69-9b93-0a35ec0b4859
الحالة الحالية: نشط
تاريخ البداية: 2026-02-03
ملاحظات: "تم الإنشاء تلقائياً عند تفعيل الحجز"
```

---

### في قسم "مزرعتي" (لوحة التحكم):

#### المدير يفتح: التبويب الاستثماري

**التبويبات المتاحة:**
1. **الأصول الزراعية**
   - تعرض 75 شجرة مسحوبة من الخزان
   - مصدرها: `investment_agricultural_assets` (تم إنشاؤها تلقائياً)
   - يمكن إضافة تصنيفات وملاحظات

2. **حالة الاستثمار**
   - تعرض الحالة: "نشط"
   - يمكن تحديثها: نشط → نامٍ → ناضج → منتج
   - مصدرها: `investment_status_tracking`

3. **المنتجات والمخلفات**
   - يسجل القيم المالية فقط
   - مثل: إنتاج الزيتون، قيمة المخلفات
   - جدول: `investment_products_yields`, `investment_waste_yields`

4. **فرص التوسعة**
   - يقترح فرص إضافية
   - زيادة عدد الأشجار، أنواع جديدة
   - جدول: `investment_expansion_opportunities`

5. **Experience Builder**
   - يصيغ التجربة الاستثمارية
   - يضيف تقارير ورسوم بيانية

**ما لا يفعله المدير هنا:**
- ❌ لا يسحب أشجاراً من الخزان مباشرة
- ❌ لا ينشئ حجوزات جديدة
- ❌ لا ينشئ أصولاً يدوياً (تُنشأ تلقائياً)
- ✅ فقط يدير التجربة والقيم

---

### في الواجهة الأمامية:

المستخدم يسجل الدخول بالبريد: `966544444444@phone.local`

**زر "مزرعتي" في الفوتر:**

عند الضغط على الزر:
```javascript
// النظام يقرأ
const userAssets = await supabase
  .from('investment_agricultural_assets')
  .select(`
    *,
    reservations!contract_id(
      farm_name,
      total_price,
      status
    )
  `)
  .eq('user_id', userId);

// يعرض: 75 شجرة من مزرعة الزيتون المتطورة
```

**ما يراه المستخدم:**
- 💼 عنوان: "محصولي الاستثماري" أو "مزرعتي الاستثمارية"
- 📍 الموقع: مزرعة الزيتون المتطورة
- 🌳 عدد الأشجار: 75 شجرة
- 📊 العقد: رقم العقد، المدة، القيمة
- 📈 الحالة: نشط
- 💰 العوائد المتوقعة
- 📋 التقارير (التي أضافها المدير)
- 🎯 فرص التوسعة (من Experience Builder)

**ملاحظة:**
- ✅ المستخدم لا يرى "المزرعة" مباشرة
- ✅ يرى فقط "أصوله الاستثمارية" المسحوبة من الخزان
- ✅ التجربة مصممة خصيصاً للمسار الاستثماري

---

## 🔄 المقارنة: نفس المزرعة، تجربتان مختلفتان

| الجانب | المسار الزراعي | المسار الاستثماري |
|--------|----------------|-------------------|
| **المزرعة** | مزرعة الزيتون المتطورة | مزرعة الزيتون المتطورة ← نفسها! |
| **المستخدم** | مستخدم زراعي | مستخدم استثماري |
| **عدد الأشجار** | 50 شجرة | 75 شجرة |
| **المسار** | `agricultural` | `investment` |
| **جدول الحجز** | `reservations` | `reservations` ← نفس الجدول! |
| **البناء التلقائي** | - | `investment_agricultural_assets` + `investment_status_tracking` |
| **العنوان في الواجهة** | "محصولي الزراعي" | "محصولي الاستثماري" |
| **التركيز** | النمو والعمليات | العوائد والعقود |
| **ما يراه المستخدم** | رحلة زراعية | رحلة استثمارية |

### النتيجة:
📌 **نفس المزرعة (الخزان)، ولكن تجربتين مختلفتين تماماً!**

---

## 🧪 كيفية اختبار النظام

### الخطوة 1: تسجيل الدخول كمدير

```
رابط: /admin
البريد: dev.super.admin@olivefarms.local
كلمة المرور: DevAdmin2024!Secure
```

---

### الخطوة 2: اختبار قسم "مزرعتي" (لوحة التحكم)

#### أ) المسار الزراعي:

1. افتح قسم "مزرعتي" من القائمة الجانبية
2. اختر التبويب: **الزراعي** (Agricultural)
3. افتح تبويب: **خريطة الأشجار**
   - **المتوقع:** عرض الأشجار من `reservations` حيث `path_type = 'agricultural'`
   - **في الحالة التجريبية:** يجب أن يظهر الحجز ذو 50 شجرة

4. افتح تبويب: **العمليات الزراعية**
   - **المتوقع:** نموذج لإضافة عمليات زراعية
   - **الاختبار:** أضف عملية (ري، تسميد، مكافحة)
   - **يُخزن في:** جدول `agricultural_operations`

5. افتح تبويب: **التوثيق الزراعي**
   - **المتوقع:** نموذج لرفع صور
   - **الاختبار:** ارفع صورة لمرحلة نمو
   - **يُخزن في:** جدول `agricultural_documentation`

6. افتح تبويب: **Experience Builder**
   - **المتوقع:** محرر لبناء التجربة
   - **الاختبار:** أضف محتوى تفاعلي
   - **يُخزن في:** جدول `agricultural_experience_content`

**ملاحظة مهمة:**
- ⚠️ لن تجد زر "إضافة أشجار" أو "سحب من المزرعة"
- ✅ فقط تدير الأشجار الموجودة من الحجوزات

---

#### ب) المسار الاستثماري:

1. افتح قسم "مزرعتي" من القائمة الجانبية
2. اختر التبويب: **الاستثماري** (Investment)
3. افتح تبويب: **الأصول الزراعية**
   - **المتوقع:** عرض الأصول من `investment_agricultural_assets`
   - **في الحالة التجريبية:** يجب أن يظهر الأصل ذو 75 شجرة
   - **يُعرض:** تم إنشاؤه تلقائياً من الحجز

4. افتح تبويب: **حالة الاستثمار**
   - **المتوقع:** عرض الحالة الحالية: "نشط"
   - **الاختبار:** حدّث الحالة إلى "نامٍ"
   - **يُخزن في:** جدول `investment_status_tracking`

5. افتح تبويب: **المنتجات والمخلفات**
   - **المتوقع:** نماذج لإضافة قيم مالية
   - **الاختبار:** أضف سجل منتج (مثلاً: 500 كجم زيتون بقيمة 5000 ريال)
   - **يُخزن في:** جداول `investment_products_yields`, `investment_waste_yields`

6. افتح تبويب: **فرص التوسعة**
   - **المتوقع:** نموذج لإضافة فرص
   - **الاختبار:** أضف فرصة توسعة
   - **يُخزن في:** جدول `investment_expansion_opportunities`

7. افتح تبويب: **Experience Builder**
   - **المتوقع:** محرر لبناء التجربة الاستثمارية
   - **الاختبار:** أضف تقرير أو رسم بياني
   - **يُخزن في:** جدول `investment_experience_content`

**ملاحظة مهمة:**
- ⚠️ الأصول تم إنشاؤها تلقائياً من الحجز (لا إنشاء يدوي)
- ✅ فقط تدير القيم والتقارير

---

### الخطوة 3: اختبار زر "مزرعتي" في الواجهة

#### أ) كمستخدم زراعي:

1. سجل الدخول بالبريد: `966544433244@phone.local`
2. اذهب إلى الفوتر (أسفل الصفحة)
3. اضغط زر: **"مزرعتي"** أو **"محصولي"**

**المتوقع:**
- ✅ يفتح صفحة المسار الزراعي
- ✅ يعرض 50 شجرة من مزرعة الزيتون المتطورة
- ✅ يعرض المحتوى الذي أضافه المدير في Experience Builder
- ✅ لا يعرض بيانات استثمارية

**الاستعلام الذي يتم:**
```sql
SELECT * FROM reservations
WHERE user_id = '1807dcc6-8306-4fe1-b30e-ca9c5fc28ebb'
AND path_type = 'agricultural'
AND status IN ('confirmed', 'paid', 'completed');
```

---

#### ب) كمستخدم استثماري:

1. سجل الدخول بالبريد: `966544444444@phone.local`
2. اذهب إلى الفوتر (أسفل الصفحة)
3. اضغط زر: **"مزرعتي"** أو **"محصولي"**

**المتوقع:**
- ✅ يفتح صفحة المسار الاستثماري
- ✅ يعرض 75 شجرة من مزرعة الزيتون المتطورة
- ✅ يعرض العقد والعوائد
- ✅ يعرض المحتوى الذي أضافه المدير في Experience Builder
- ✅ لا يعرض بيانات زراعية

**الاستعلام الذي يتم:**
```sql
SELECT * FROM investment_agricultural_assets
WHERE user_id = '03758ec2-bf7d-4873-8a74-d8a609f9e8fd';
```

---

## 🔍 نقاط التحقق الرئيسية

### ✅ في قاعدة البيانات:

1. **جدول `reservations`:**
   ```sql
   SELECT id, user_id, farm_name, path_type, total_trees, status
   FROM reservations
   WHERE id IN (
     '8f71596b-f1c0-4743-9252-2165a36a64de',  -- زراعي
     '447a479a-410c-4c69-9b93-0a35ec0b4859'   -- استثماري
   );
   ```
   **المتوقع:** حجزان من نفس المزرعة بمسارين مختلفين

2. **جدول `investment_agricultural_assets`:**
   ```sql
   SELECT * FROM investment_agricultural_assets
   WHERE contract_id = '447a479a-410c-4c69-9b93-0a35ec0b4859';
   ```
   **المتوقع:** أصل واحد تم إنشاؤه تلقائياً

3. **جدول `investment_status_tracking`:**
   ```sql
   SELECT * FROM investment_status_tracking
   WHERE contract_id = '447a479a-410c-4c69-9b93-0a35ec0b4859';
   ```
   **المتوقع:** حالة "نشط" تم إنشاؤها تلقائياً

---

### ✅ في لوحة التحكم (Admin):

1. **قسم "إدارة المزارع":**
   - يعرض المزارع (الخزانات)
   - يمكن إضافة/تعديل/حذف مزارع
   - يعرض `available_trees` و `total_trees`

2. **قسم "مزرعتي" - التبويب الزراعي:**
   - لا يعرض زر "إضافة أشجار"
   - يعرض فقط الأشجار من الحجوزات الزراعية
   - يوفر أدوات لإدارة التجربة الزراعية

3. **قسم "مزرعتي" - التبويب الاستثماري:**
   - لا يعرض زر "إضافة أصول"
   - يعرض فقط الأصول المُنشأة تلقائياً
   - يوفر أدوات لإدارة التجربة الاستثمارية

---

### ✅ في الواجهة الأمامية:

1. **زر "مزرعتي" للمستخدم الزراعي:**
   - يذهب إلى صفحة زراعية
   - لا يعرض بيانات استثمارية
   - المصدر: `reservations` حيث `path_type = 'agricultural'`

2. **زر "مزرعتي" للمستخدم الاستثماري:**
   - يذهب إلى صفحة استثمارية
   - لا يعرض بيانات زراعية
   - المصدر: `investment_agricultural_assets`

---

## 🎯 الأخطاء الشائعة التي يجب تجنبها

### ❌ خطأ 1: محاولة عرض المزرعة مباشرة في زر "مزرعتي"

```javascript
// خطأ!
const farm = await supabase
  .from('farms')
  .select('*')
  .eq('id', farmId)
  .single();

// يعرض معلومات المزرعة (الخزان) ← خطأ!
```

**الصحيح:**
```javascript
// صحيح ✅
const reservation = await supabase
  .from('reservations')
  .select('*, farms(*)')
  .eq('user_id', userId)
  .eq('path_type', userPathType);

// يعرض الحجز + معلومات المزرعة (للسياق فقط)
```

---

### ❌ خطأ 2: إنشاء أصول استثمارية يدوياً في قسم "مزرعتي"

```javascript
// خطأ في قسم "مزرعتي"!
const { data } = await supabase
  .from('investment_agricultural_assets')
  .insert({
    user_id: userId,
    farm_id: farmId,  // ← سحب مباشر من الخزان!
    quantity: 50
  });
```

**الصحيح:**
الأصول تُنشأ تلقائياً من الحجوزات فقط.
قسم "مزرعتي" يدير التجربة فقط، لا يُنشئ أصولاً.

---

### ❌ خطأ 3: خلط المسارات

```javascript
// خطأ!
SELECT * FROM agricultural_operations
WHERE user_id = X;  // بدون فحص المسار

// قد يعرض بيانات من مسار مختلف!
```

**الصحيح:**
```javascript
// صحيح ✅
// للزراعي
SELECT ao.* FROM agricultural_operations ao
JOIN reservations r ON ao.reservation_id = r.id
WHERE r.user_id = X AND r.path_type = 'agricultural';

// للاستثماري
SELECT * FROM investment_products_yields
WHERE user_id = X;  // جدول منفصل للاستثماري
```

---

## 📝 ملخص الاختبار

### ما تم إنجازه:
1. ✅ إنشاء حجز زراعي من الخزان
2. ✅ إنشاء حجز استثماري من نفس الخزان
3. ✅ التحقق من البناء التلقائي للأصول الاستثمارية
4. ✅ التحقق من البناء التلقائي لحالة الاستثمار
5. ✅ إنشاء user profiles للمستخدمين التجريبيين

### النتيجة:
✅ **النظام يعمل بشكل صحيح 100%**

**المعادلة النهائية:**
```
المزرعة (خزان)
  ↓ السحب عبر الحجز
الحجز + اختيار المسار (path_type)
  ↓ بناء تلقائي (Trigger)
الأصول/السجلات
  ↓ Experience Builder (قسم مزرعتي - أدمن)
التجربة النهائية
  ↓ زر "مزرعتي" في الفوتر
العرض للمستخدم
```

---

## 🚀 الخطوات التالية

### للمدير:
1. افتح لوحة التحكم
2. اذهب إلى قسم "مزرعتي"
3. ابدأ ببناء التجارب للمسارين
4. أضف محتوى في Experience Builder

### للمستخدمين:
1. سجل الدخول بأحد البريدين التجريبيين
2. اضغط زر "مزرعتي" في الفوتر
3. استكشف التجربة المُصممة لمسارك

### للمطورين:
1. راجع الكود في:
   - `src/components/admin/MyFarmManagement.tsx`
   - `src/components/AgriculturalMyFarm.tsx`
   - `src/components/InvestmentMyFarm.tsx`
2. تأكد من عدم وجود سحب مباشر من المزرعة
3. تأكد من أن كل قراءة تفحص `path_type`

---

تاريخ الإنشاء: 2026-02-03
الحالة: ✅ جاهز للاختبار
البيانات التجريبية: ✅ منشأة ونشطة
