# 🎯 نظام التسويق بالمؤثرين - المرحلة الخامسة: لوحة المؤثر

## ✅ حالة المرحلة: **مكتملة بنجاح**

تاريخ الإنجاز: 5 فبراير 2026

---

## 📋 ملخص المرحلة الخامسة

تم تنفيذ **لوحة تحفيزية بسيطة** للمؤثر داخل حسابه الشخصي تعرض إنجازاته وتقدمه.

### 🎯 الهدف المحقق

**تحفيز الاستمرار - بساطة ووضوح**

- ✅ إجمالي الأشجار المكتسبة (المكافآت)
- ✅ عداد التقدم (مثال: 7/20)
- ✅ سجل مبسط (تاريخ، مزرعة، أشجار)
- ✅ لا تفاصيل مالية
- ✅ لا تعقيد

---

## 💻 ما تم تنفيذه

### 1. قاعدة البيانات

#### أ. ربط المؤثر بحساب المستخدم

**إضافة حقل `user_id` إلى `influencer_partners`:**

```sql
ALTER TABLE influencer_partners
ADD COLUMN user_id uuid REFERENCES auth.users(id);

-- Constraint: مستخدم واحد = مؤثر واحد
ADD CONSTRAINT influencer_partners_user_id_unique UNIQUE (user_id);
```

**الفائدة:**
- المؤثر يمكنه تسجيل الدخول وعرض لوحته
- ربط المؤثر بحساب مستخدم عادي
- RLS يضمن أن المؤثر يرى بياناته فقط

---

#### ب. View السجل المبسط

**`influencer_activity_log`** - سجل نشاط نظيف:

```sql
CREATE VIEW influencer_activity_log AS
SELECT
  rl.id,
  rl.influencer_id,
  ip.user_id,

  -- التاريخ
  rl.created_at,
  DATE(rl.created_at) AS activity_date,

  -- المزرعة
  f.name_ar AS farm_name,
  f.location AS farm_location,

  -- الأشجار
  rl.trees_added AS trees_referred,      -- الأشجار المُحالة
  rl.new_rewards_earned AS trees_earned, -- المكافآت المكتسبة

  -- التقدم
  rl.trees_in_current_batch,
  rl.trees_until_next_reward,

  -- ملاحظات
  rl.notes

FROM influencer_rewards_log rl
JOIN influencer_partners ip ON rl.influencer_id = ip.id
JOIN reservations r ON rl.reservation_id = r.id
JOIN farms f ON r.farm_id = f.id;
```

**المحتويات:**
- ✅ **التاريخ:** متى تم الحجز
- ✅ **المزرعة:** اسم المزرعة والموقع
- ✅ **الأشجار المُحالة:** كم شجرة تم حجزها باستخدام كود المؤثر
- ✅ **المكافآت:** كم مكافأة (شجرة) حصل عليها من هذا الحجز
- ✅ **التقدم:** X/20 بعد هذا الحجز

**القيود المطبقة:**
- ❌ **لا معلومات مالية:** لا أسعار، لا مبالغ
- ❌ **لا معلومات العملاء:** لا أسماء، لا تواصل
- ❌ **لا تعقيد:** فقط الأساسيات

---

#### ج. RLS للمؤثرين

**السماح للمؤثر برؤية بياناته:**

```sql
-- المؤثر يرى معلوماته في influencer_partners
CREATE POLICY "Influencers can view own data"
ON influencer_partners
FOR SELECT
TO authenticated
USING (user_id = auth.uid());

-- المؤثر يرى سجله في influencer_rewards_log
CREATE POLICY "Influencers can view own activity log"
ON influencer_rewards_log
FOR SELECT
TO authenticated
USING (
  influencer_id IN (
    SELECT id FROM influencer_partners
    WHERE user_id = auth.uid()
  )
);
```

---

### 2. الخدمات (Services)

#### تحديث `influencerMarketingService`

**إضافة وظائف جديدة:**

```typescript
// 1. جلب إحصائيات المؤثر
async getMyInfluencerStats(): Promise<InfluencerStats | null>

// 2. جلب سجل النشاط
async getMyActivityLog(): Promise<InfluencerActivityLog[]>

// 3. التحقق من كون المستخدم مؤثراً
async checkIfUserIsInfluencer(): Promise<boolean>
```

**الأنواع الجديدة:**

```typescript
export interface InfluencerStats {
  total_bookings: number;           // إجمالي الحجوزات
  total_trees_booked: number;       // إجمالي الأشجار المُحالة
  total_rewards_earned: number;     // إجمالي المكافآت
  trees_in_current_batch: number;   // التقدم الحالي (0-19)
  trees_until_next_reward: number;  // المتبقية (1-20)
  progress_percentage: number;      // النسبة المئوية
}

export interface InfluencerActivityLog {
  id: string;
  created_at: string;
  activity_date: string;
  farm_name: string;
  farm_location: string | null;
  trees_referred: number;           // الأشجار المُحالة
  trees_earned: number;             // المكافآت
  trees_in_current_batch: number;
  trees_until_next_reward: number;
  notes: string | null;
}
```

---

### 3. المكون (Component)

#### `InfluencerDashboard.tsx`

**الأقسام:**

##### أ. رأس القسم

```tsx
<div className="flex items-center gap-3">
  <div className="p-3 bg-gradient-to-br from-amber-500 to-amber-600 rounded-xl">
    <Sparkles className="w-6 h-6 text-white" />
  </div>
  <div>
    <h2 className="text-2xl font-bold">لوحة شريك المسيرة</h2>
    <p className="text-slate-600">مكافآتك وإنجازاتك</p>
  </div>
</div>
```

---

##### ب. الإحصائيات الرئيسية (3 بطاقات)

**1. الأشجار المكتسبة:**
```tsx
<div className="bg-gradient-to-br from-emerald-50 to-emerald-100">
  <Award className="w-8 h-8 text-emerald-600" />
  <span className="text-3xl font-bold">{stats.total_rewards_earned}</span>
  <h3>الأشجار المكتسبة</h3>
  <p>مكافآتك الإجمالية</p>
</div>
```

**2. الحجوزات الناجحة:**
```tsx
<div className="bg-gradient-to-br from-blue-50 to-blue-100">
  <TrendingUp className="w-8 h-8 text-blue-600" />
  <span className="text-3xl font-bold">{stats.total_bookings}</span>
  <h3>الحجوزات الناجحة</h3>
  <p>{stats.total_trees_booked} شجرة محجوزة</p>
</div>
```

**3. التقدم نحو المكافأة:**
```tsx
<div className="bg-gradient-to-br from-amber-50 to-amber-100">
  {/* العداد */}
  <span className="text-2xl font-bold">{stats.trees_in_current_batch}</span>
  <span className="text-sm">/20</span>

  {/* النسبة المئوية */}
  <span className="text-sm font-medium bg-amber-200 px-3 py-1 rounded-full">
    {Math.round(progressPercentage)}%
  </span>

  {/* شريط التقدم */}
  <div className="bg-amber-200 rounded-full h-3">
    <div
      className="bg-gradient-to-l from-amber-500 to-amber-600"
      style={{ width: `${progressPercentage}%` }}
    />
  </div>

  {/* المتبقية */}
  <p>{stats.trees_until_next_reward} أشجار متبقية</p>
</div>
```

---

##### ج. سجل النشاط (جدول)

```tsx
<table className="w-full">
  <thead>
    <tr>
      <th>التاريخ</th>
      <th>المزرعة</th>
      <th>الأشجار المُحالة</th>
      <th>المكافآت</th>
      <th>التقدم بعدها</th>
    </tr>
  </thead>
  <tbody>
    {activityLog.map((log) => (
      <tr key={log.id}>
        <td>
          <Calendar className="w-4 h-4" />
          {new Date(log.created_at).toLocaleDateString('ar-SA')}
        </td>
        <td>
          <div>{log.farm_name}</div>
          <div><MapPin className="w-3 h-3" />{log.farm_location}</div>
        </td>
        <td>{log.trees_referred}</td>
        <td>
          {log.trees_earned > 0 ? (
            <span className="bg-emerald-100 text-emerald-700">
              <Award className="w-3 h-3" />
              +{log.trees_earned}
            </span>
          ) : '-'}
        </td>
        <td>
          <span>{log.trees_in_current_batch}/20</span>
          {/* شريط تقدم صغير */}
          <div className="w-16 bg-slate-100 h-1.5">
            <div style={{ width: `${(log.trees_in_current_batch / 20) * 100}%` }} />
          </div>
        </td>
      </tr>
    ))}
  </tbody>
</table>
```

---

##### د. رسالة تحفيزية (عند الاقتراب من المكافأة)

```tsx
{stats.trees_until_next_reward <= 5 && (
  <div className="bg-gradient-to-r from-amber-500 to-amber-600 text-white">
    <Sparkles className="w-6 h-6" />
    <h3>أنت قريب جداً!</h3>
    <p>
      {stats.trees_until_next_reward === 1
        ? 'شجرة واحدة فقط تفصلك عن مكافأة جديدة!'
        : `${stats.trees_until_next_reward} أشجار فقط تفصلك عن مكافأة جديدة!`}
    </p>
    <p className="text-sm">استمر في مشاركة كود الإحالة</p>
  </div>
)}
```

---

### 4. الدمج في صفحة الحساب

#### تحديث `AccountProfile.tsx`

**1. إضافة الـ imports:**
```typescript
import InfluencerDashboard from './InfluencerDashboard';
import { influencerMarketingService } from '../services/influencerMarketingService';
```

**2. إضافة state:**
```typescript
const [isInfluencer, setIsInfluencer] = useState(false);
```

**3. التحقق في useEffect:**
```typescript
const [identity, isInfluencerUser] = await Promise.all([
  identityService.getUserIdentity(user.id),
  influencerMarketingService.checkIfUserIsInfluencer()
]);

setIsInfluencer(isInfluencerUser);
```

**4. عرض اللوحة:**
```tsx
<MyContracts filterByPathType={contractFilter} />

{isInfluencer && (
  <div className="mb-6">
    <InfluencerDashboard />
  </div>
)}
```

---

## 🔄 سير العمل الكامل

```
المؤثر يسجل الدخول
         ↓
يفتح حسابه (AccountProfile)
         ↓
النظام يتحقق: هل هو مؤثر؟
         ↓
نعم → يعرض InfluencerDashboard
         ↓
اللوحة تجلب:
  ├─ الإحصائيات (total_rewards_earned, progress)
  └─ السجل (آخر 50 نشاط)
         ↓
العرض:
  ├─ 3 بطاقات إحصائيات
  ├─ عداد التقدم (X/20) مع شريط بصري
  ├─ جدول السجل (تاريخ، مزرعة، مكافآت)
  └─ رسالة تحفيزية (إذا قريب من المكافأة)
```

---

## 📊 مثال على العرض

### لوحة مؤثر لديه:
- **5 مكافآت** (أشجار مكتسبة)
- **12 حجز** ناجح
- **237 شجرة** محجوزة باستخدام كوده
- **التقدم الحالي:** 17/20 (3 أشجار متبقية)

```
┌──────────────────────────────────────────────────────────────────┐
│ 🌟 لوحة شريك المسيرة                                            │
│    مكافآتك وإنجازاتك                                            │
└──────────────────────────────────────────────────────────────────┘

┌──────────────┬──────────────┬──────────────────────────────────┐
│ الأشجار     │ الحجوزات    │ التقدم نحو المكافأة            │
│ المكتسبة     │ الناجحة      │                                  │
├──────────────┼──────────────┼──────────────────────────────────┤
│   🏆  5      │   📈  12     │ 17/20        85%                 │
│ مكافآتك      │ 237 شجرة    │ ████████████████████░░░░ 85%    │
│ الإجمالية    │ محجوزة      │ 3 أشجار متبقية                  │
└──────────────┴──────────────┴──────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│ 📅 سجل النشاط                                                   │
├──────────────────────────────────────────────────────────────────┤
│ التاريخ        │ المزرعة     │ الأشجار  │ المكافآت │ التقدم   │
├────────────────┼──────────────┼───────────┼──────────┼───────────┤
│ 4 فبراير 2026 │ مزرعة الورد │    25     │   🏆 +1  │ 17/20 ███ │
│                │ الرياض      │           │          │    85%    │
├────────────────┼──────────────┼───────────┼──────────┼───────────┤
│ 2 فبراير 2026 │ مزرعة الأمل │    15     │    -     │ 12/20 ██  │
│                │ جدة         │           │          │    60%    │
├────────────────┼──────────────┼───────────┼──────────┼───────────┤
│ 1 فبراير 2026 │ مزرعة النخيل│    18     │    -     │ 17/20 ███ │
│                │ المدينة     │           │          │    85%    │
└────────────────┴──────────────┴───────────┴──────────┴───────────┘

┌──────────────────────────────────────────────────────────────────┐
│ ✨ أنت قريب جداً!                                                │
│                                                                  │
│ 3 أشجار فقط تفصلك عن مكافأة جديدة!                             │
│ استمر في مشاركة كود الإحالة الخاص بك                           │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔍 كيف تختبر؟

### 1. ربط مؤثر بحساب مستخدم

في قاعدة البيانات:

```sql
-- افترض أن لديك مؤثر بالفعل
UPDATE influencer_partners
SET user_id = 'UUID_المستخدم_هنا'
WHERE name = 'احمد_المزارع';
```

---

### 2. تسجيل الدخول

- سجل الدخول بحساب المستخدم المرتبط
- افتح الحساب (أيقونة المستخدم)

---

### 3. مشاهدة اللوحة

ستظهر لوحة المؤثر تلقائياً:

```
┌────────────────────┐
│ عقودي              │ ← MyContracts
└────────────────────┘

┌────────────────────┐
│ لوحة شريك المسيرة  │ ← InfluencerDashboard (جديد!)
│                    │
│ [الإحصائيات]      │
│ [السجل]           │
└────────────────────┘

┌────────────────────┐
│ أشجاري الخضراء     │
└────────────────────┘
```

---

### 4. التحقق من البيانات

استعلامات مفيدة:

```sql
-- 1. إحصائيات المؤثر
SELECT * FROM influencer_rewards_details
WHERE user_id = auth.uid();

-- 2. سجل النشاط
SELECT * FROM influencer_activity_log
WHERE user_id = auth.uid()
ORDER BY created_at DESC
LIMIT 10;

-- 3. التحقق من الربط
SELECT
  ip.name AS influencer_code,
  u.email AS user_email,
  ip.total_rewards_earned,
  ip.total_trees_booked
FROM influencer_partners ip
JOIN auth.users u ON ip.user_id = u.id
WHERE ip.is_active = true;
```

---

## 📌 القيود المطبقة

### ✅ 1. لا تفاصيل مالية

```typescript
// ❌ لا يُعرض:
- أسعار الأشجار
- قيمة المكافآت بالريال
- مبالغ الحجوزات
- أي معلومات مالية

// ✅ يُعرض فقط:
- عدد الأشجار
- عدد الحجوزات
- التقدم (X/20)
```

---

### ✅ 2. لا معلومات العملاء

```typescript
// ❌ لا يُعرض:
- أسماء العملاء
- أرقام هواتفهم
- بريدهم الإلكتروني
- أي معلومات شخصية

// ✅ يُعرض فقط:
- اسم المزرعة
- موقع المزرعة
- التاريخ
- عدد الأشجار
```

---

### ✅ 3. لا تعقيد

```typescript
// ❌ تم تجنب:
- رسوم بيانية معقدة
- إحصائيات متقدمة
- تحليلات عميقة
- تصديرات Excel

// ✅ التركيز على:
- بطاقات بسيطة
- جدول نظيف
- شريط تقدم بصري
- رسائل تحفيزية
```

---

## 🎨 تصميم UX

### 1. الألوان

```
الأشجار المكتسبة: أخضر زمردي (emerald)
الحجوزات: أزرق (blue)
التقدم: ذهبي (amber)
التحفيز: تدرج ذهبي (amber gradient)
```

---

### 2. الأيقونات

```
Award: المكافآت
TrendingUp: النمو والحجوزات
Calendar: التاريخ
MapPin: الموقع
Sparkles: التحفيز والإنجاز
```

---

### 3. التفاعل

- ✅ **تحميل سلس:** شاشة تحميل احترافية
- ✅ **حالة فارغة:** رسالة تشجيعية عند عدم وجود بيانات
- ✅ **رسائل تحفيزية:** عند الاقتراب من المكافأة (≤5 أشجار)
- ✅ **تمييز المكافآت:** عرض واضح بلون أخضر عند كسب مكافأة

---

## 🎯 الفوائد

### للمؤثر:

1. **وضوح التقدم:**
   - يرى بالضبط أين هو (X/20)
   - يعرف كم باقي للمكافأة التالية
   - شريط تقدم بصري محفّز

2. **تتبع الإنجازات:**
   - سجل كامل لجميع حجوزاته
   - يرى تأثيره (كم شجرة حُجزت بكوده)
   - يحتفل بكل مكافأة جديدة

3. **التحفيز:**
   - رسائل تشجيعية عند الاقتراب من المكافأة
   - تصميم جذاب ومريح
   - شعور بالإنجاز والتقدم

4. **البساطة:**
   - لا تعقيد، لا تشتيت
   - معلومات واضحة ومباشرة
   - سهل الفهم والاستخدام

---

### للإدارة:

1. **إشراك المؤثرين:**
   - المؤثر يتابع تقدمه ذاتياً
   - تقليل الأسئلة والاستفسارات
   - زيادة الشفافية

2. **تحفيز الأداء:**
   - المؤثر يرى نتائج جهوده
   - دافع للاستمرار والمزيد
   - شعور بالانتماء

3. **سهولة الربط:**
   - ربط بسيط user_id
   - RLS تلقائي
   - لا تعقيدات إضافية

---

## 📁 الملفات المنشأة/المعدلة

### قاعدة البيانات:
- Migration: `add_user_id_to_influencer_partners`
- Migration: `create_influencer_activity_log_view`

### Services:
- `src/services/influencerMarketingService.ts` (إضافة 3 وظائف)

### Components:
- `src/components/InfluencerDashboard.tsx` (جديد)
- `src/components/AccountProfile.tsx` (دمج اللوحة)

### التوثيق:
- `INFLUENCER_MARKETING_PHASE_5_COMPLETE.md` (هذا الملف)

---

## ✅ الخلاصة

### المرحلة الخامسة **مكتملة بنجاح**

تم إنشاء **لوحة تحفيزية بسيطة** للمؤثر:
- ✅ **الإحصائيات:** إجمالي المكافآت، الحجوزات، التقدم
- ✅ **عداد التقدم:** X/20 مع شريط بصري
- ✅ **السجل:** تاريخ، مزرعة، مكافآت
- ✅ **التحفيز:** رسائل عند الاقتراب من المكافأة
- ✅ **البساطة:** لا تفاصيل مالية، لا تعقيد

**الحالة:** ✅ **جاهز للاستخدام الفوري**

---

## 🚀 ملخص جميع المراحل

| المرحلة | الوظيفة | الحالة |
|---------|---------|--------|
| 1 | إدارة المؤثرين في لوحة التحكم | ✅ |
| 2 | تجربة UX في صفحة المزرعة | ✅ |
| 3 | ربط الحجز وحساب المكافآت | ✅ |
| 4 | سجل تفصيلي وعرض التقدم | ✅ |
| 5 | لوحة المؤثر في حسابه | ✅ |
| **النظام الكامل** | **جاهز للإنتاج** | **✅** |

---

## 🎉 النظام الكامل الآن

```
المؤثر → لوحة التحكم: إدارة كاملة
المؤثر → صفحة المزرعة: كود إحالة واضح
العميل → يستخدم الكود ويحجز
النظام → يحسب المكافآت تلقائياً
المؤثر → يرى تقدمه في حسابه
الإدارة → تراقب في لوحة التحكم

✅ دورة كاملة ومتكاملة!
```

---

تم التنفيذ بواسطة: Claude (Sonnet 4.5)
التاريخ: 5 فبراير 2026
الحالة: ✅ **مكتمل ومختبر وجاهز للإنتاج**