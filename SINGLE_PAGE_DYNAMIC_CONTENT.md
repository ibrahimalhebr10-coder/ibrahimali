# صفحة واحدة - محتوى ديناميكي

## نظرة عامة

تم إعادة تصميم حساب المستثمر ليكون صفحة واحدة مع محتوى ديناميكي يتغير حسب حالة الرحلة الاستثمارية. كل حالة لها زر واحد فقط (أو لا زر) لمنع التشتيت والتركيز على الإجراء المطلوب.

---

## الفلسفة

### المشكلة قبل التحديث
- views متعددة منفصلة
- تكرار كود
- صعوبة الصيانة
- احتمال وجود أكثر من CTA في الصفحة

### الحل بعد التحديث
- صفحة واحدة موحدة
- محتوى ديناميكي يتغير حسب الحالة
- زر واحد فقط (أو لا زر)
- بنية منظمة وسهلة الصيانة

---

## البنية المعمارية

### 1. المكون الرئيسي: `InvestorAccount`

مسؤول عن:
- تحميل حالة المستثمر
- إدارة عرض صفحة السداد
- عرض حالة التحميل والأخطاء

### 2. المكون الديناميكي: `InvestorAccountCard`

مسؤول عن:
- عرض المحتوى الديناميكي
- استقبال الـ config من `getStateConfig`
- عرض جميع العناصر بناءً على الـ config

### 3. دالة التكوين: `getStateConfig`

مسؤولة عن:
- تحديد المحتوى لكل حالة
- إرجاع كائن config موحد
- تحديد ما إذا كان يوجد CTA أم لا

---

## الحالات وأزرارها

### 1. لا يوجد حجز (`no_reservation`)

**العرض**:
- أيقونة شجرة خضراء
- "ابدأ رحلتك الاستثمارية"
- "لا يوجد لديك حجز بعد"

**الزر**:
```
تصفح المزارع المتاحة →
```
**الإجراء**: الانتقال للصفحة الرئيسية

---

### 2. محجوز مؤقتاً (`pending`)

**العرض**:
- شارة: "محجوز مؤقتاً باسمك" (أصفر متحرك)
- "تم حجز مزرعتك بنجاح"
- بطاقتين معلومات:
  - أشجارك محجوزة
  - مراجعة سريعة
- قسم "الخطوة التالية"

**الزر**: لا يوجد (فقط معلومات)

---

### 3. جاهز للسداد (`waiting_for_payment`)

**العرض**:
- شارة: "معتمد - جاهز للسداد" (أزرق متحرك)
- "تم اعتماد حجزك"
- قسم "بعد السداد"

**الزر**:
```
💳 انتقل للسداد →
```
**الإجراء**: فتح صفحة السداد

---

### 4. إيصال مرفوع (`payment_submitted`)

**العرض**:
- شارة: "في انتظار اعتماد السداد" (بنفسجي متحرك)
- "تم رفع إيصال السداد"
- قسم "مراجعة الإيصال" مع التاريخ

**الزر**: لا يوجد (فقط معلومات)

---

### 5. سداد معتمد (`paid`)

**العرض**:
- شارة: "تم اعتماد السداد" (أخضر ثابت)
- "مبروك! تم تأكيد سدادك"
- قسم "الخطوة التالية" (النقل لمحصولي)

**الزر**: لا يوجد (في انتظار النقل)

---

### 6. منقول لمحصولي (`transferred_to_harvest`)

**العرض**:
- شارة: "نشط في محصولي" (أخضر ثابت)
- "مبروك! أشجارك في محصولي"

**الزر**:
```
🌱 انتقل إلى محصولي →
```
**الإجراء**: الانتقال لصفحة محصولي

---

### 7. ملغي (`cancelled`)

**العرض**:
- شارة: "تم الإلغاء" (أحمر)
- "تم إلغاء هذا الحجز"

**الزر**:
```
🌲 تصفح المزارع المتاحة →
```
**الإجراء**: الانتقال للصفحة الرئيسية

---

## بنية Config

### العناصر الأساسية

```typescript
{
  borderColor: string,           // لون حدود البطاقة
  iconBg: string,               // خلفية الأيقونة (gradient)
  icon: ReactNode,              // الأيقونة الرئيسية
  title: string,                // العنوان الرئيسي
  description: string,          // الوصف
  summaryBg: string,            // خلفية ملخص الحجز
  summaryBorder: string         // حدود ملخص الحجز
}
```

### العناصر الاختيارية

#### الشارة (Badge)
```typescript
badge?: {
  text: string,               // نص الشارة
  bg: string,                 // لون الخلفية
  border: string,             // لون الحدود
  textColor: string,          // لون النص
  dotColor: string,           // لون النقطة
  animated: boolean           // متحركة أم لا
}
```

#### بطاقات المعلومات (Info Cards)
```typescript
infoCards?: Array<{
  bg: string,                 // لون الخلفية
  border: string,             // لون الحدود
  iconColor: string,          // لون الأيقونة
  titleColor: string,         // لون العنوان
  textColor: string,          // لون النص
  icon: ReactNode,            // الأيقونة
  title: string,              // العنوان
  description: string         // الوصف
}>
```

#### قسم الخطوة التالية (Next Step)
```typescript
nextStep?: {
  bg: string,                 // لون الخلفية
  border: string,             // لون الحدود
  iconColor: string,          // لون الأيقونة
  titleColor: string,         // لون العنوان
  textColor: string,          // لون النص
  icon: ReactNode,            // الأيقونة
  title: string,              // العنوان
  description: string         // الوصف
}
```

#### زر الإجراء (CTA)
```typescript
cta?: {
  text: string,               // نص الزر
  action: 'payment' | 'navigate',  // نوع الإجراء
  href?: string,              // الرابط (للانتقال)
  gradient: string,           // لون الزر (gradient)
  iconLeft: ReactNode | null, // أيقونة يسار
  iconRight: ReactNode        // أيقونة يمين
}
```

---

## المزايا

### 1. بساطة الكود
- مكون واحد فقط
- لا تكرار
- سهولة الصيانة

### 2. تجربة مستخدم محسّنة
- صفحة واحدة واضحة
- زر واحد فقط
- لا تشتيت

### 3. مرونة عالية
- إضافة حالات جديدة سهلة
- تعديل المحتوى سهل
- تخصيص الألوان مرن

### 4. اتساق التصميم
- نفس البنية لجميع الحالات
- ألوان موحدة ومتناسقة
- تجربة متسقة

---

## نظام الألوان

### الأخضر (Green)
- **الاستخدام**: نجاح، إكمال، تأكيد
- **الحالات**: no_reservation, pending, paid
- **الهدف**: طمأنينة وإيجابية

### الأزرق (Blue)
- **الاستخدام**: جاهز للإجراء، معتمد
- **الحالات**: waiting_for_payment
- **الهدف**: ثقة واستقرار

### البنفسجي (Purple)
- **الاستخدام**: انتظار، مراجعة
- **الحالات**: payment_submitted
- **الهدف**: معلومة، عملية جارية

### الزمردي (Emerald/Teal)
- **الاستخدام**: نشط، حي، نمو
- **الحالات**: transferred_to_harvest
- **الهدف**: حيوية ونمو

### الأحمر (Red)
- **الاستخدام**: إلغاء، خطأ
- **الحالات**: cancelled
- **الهدف**: تنبيه ووضوح

### الأصفر (Amber)
- **الاستخدام**: مؤقت، في انتظار
- **الحالات**: شارات الحالات المؤقتة
- **الهدف**: جذب الانتباه دون قلق

---

## الرسوم المتحركة

### 1. شارات الحالة
- نقطة متحركة للحالات المؤقتة
- ثابتة للحالات النهائية

### 2. الأزرار
```css
hover:scale-105
transition-all
transform
```

### 3. أيقونة التحميل
```
animate-spin
```

---

## Responsive Design

### Mobile (< 768px)
- عمود واحد للبطاقات
- أزرار بعرض كامل
- نص بحجم مناسب

### Desktop (≥ 768px)
- عمودين للبطاقات
- أزرار بعرض كامل
- نص بحجم أكبر قليلاً

---

## التكامل مع الخدمات

### investorJourneyService
- `getInvestorState()`: الحصول على حالة المستخدم
- `getStatusLabel()`: الحصول على نص الحالة
- `getStatusColor()`: الحصول على ألوان الحالة

### PaymentPage
- يتم عرضها عند النقر على "انتقل للسداد"
- لها callbacks للإغلاق والإكمال

---

## أمثلة الاستخدام

### إضافة حالة جديدة

```typescript
case 'new_state':
  return {
    borderColor: '#...',
    iconBg: 'linear-gradient(...)',
    icon: <Icon className="..." />,
    title: 'العنوان',
    description: 'الوصف',
    summaryBg: 'linear-gradient(...)',
    summaryBorder: '#...',
    cta: {
      text: 'نص الزر',
      action: 'navigate',
      href: '/path',
      gradient: 'linear-gradient(...)',
      iconLeft: null,
      iconRight: <ArrowRight />
    }
  };
```

### تعديل محتوى حالة موجودة

فقط قم بتعديل الـ config في `getStateConfig`:

```typescript
case 'pending':
  return {
    // ... تعديل أي عنصر هنا
    title: 'عنوان جديد',
    description: 'وصف جديد'
  };
```

---

## الاختبار

### البناء
```bash
npm run build
```
**النتيجة**: ✅ نجح بدون أخطاء

### الملفات المعدّلة
- ✅ `InvestorAccount.tsx` - إعادة كتابة كاملة

---

## إحصائيات

### قبل التحديث
- 7 مكونات منفصلة (functions)
- ~400 سطر كود
- تكرار كبير

### بعد التحديث
- 3 مكونات (InvestorAccount, InvestorAccountCard, getStateConfig)
- ~525 سطر كود (مع config مفصل)
- لا تكرار

### النتيجة
- كود أكثر تنظيماً
- صيانة أسهل
- توسع أسرع

---

## قواعد التصميم المتبعة

### 1. زر واحد فقط
- كل حالة لها زر واحد أو لا زر
- لا خيارات متعددة
- تركيز كامل على الإجراء المطلوب

### 2. لا تبويبات
- صفحة واحدة فقط
- محتوى يتغير ديناميكياً
- لا تعقيد في الانتقال

### 3. لا تشتيت
- معلومات واضحة ومحددة
- ألوان متناسقة
- تركيز على الهدف

### 4. وضوح الحالة
- شارة واضحة لكل حالة
- ألوان دالة على المعنى
- رسوم متحركة مناسبة

---

## الخلاصة

تم تحويل صفحة حساب المستثمر من نظام Views متعددة إلى صفحة واحدة موحدة مع:

1. ✅ محتوى ديناميكي يتغير حسب الحالة
2. ✅ زر واحد فقط (أو لا زر) لكل حالة
3. ✅ لا تبويبات
4. ✅ لا تشتيت
5. ✅ تصميم نظيف ومتسق
6. ✅ سهولة الصيانة والتوسع

النتيجة: تجربة مستخدم محسّنة، كود أنظف، وصيانة أسهل.
