# 🧪 دليل اختبار شريط شركاء النجاح

## ✅ الخطوات السريعة

### 1️⃣ افتح الواجهة الرئيسية
- اذهب إلى الصفحة الرئيسية للمنصة
- **لا تسجل دخول** (اختبر كمستخدم عادي)

### 2️⃣ ما يجب أن تراه:
```
┌─────────────────────────────────────────┐
│  Header (أخضر - لوجو + "حصص زراعية")   │
├─────────────────────────────────────────┤
│  🤝 انضم لبرنامج شركاء النجاح واربح...  │ ← الشريط الذهبي (هنا!)
├─────────────────────────────────────────┤
│                                         │
│         محتوى الصفحة الرئيسية           │
│                                         │
└─────────────────────────────────────────┘
```

### 3️⃣ اختبر التفاعل:
- [ ] الشريط يظهر باللون الذهبي ✅
- [ ] النص واضح: "انضم لبرنامج شركاء النجاح واربح من كل حجز!"
- [ ] أيقونة المصافحة 🤝 تظهر
- [ ] زر X يظهر على اليمين
- [ ] عند الضغط على الشريط → يفتح صفحة شركاء النجاح
- [ ] عند الضغط على X → الشريط يختفي

---

## 🔍 الاختبار التفصيلي

### اختبار 1: بدون تسجيل دخول (Anonymous)
**الهدف:** التأكد من أن الشريط يظهر للمستخدمين غير المسجلين

1. افتح المتصفح في وضع Incognito/Private
2. اذهب إلى الواجهة الرئيسية
3. **يجب أن ترى الشريط** ✅

**✅ النجاح:** الشريط يظهر
**❌ الفشل:** الشريط لا يظهر → تحقق من قاعدة البيانات

---

### اختبار 2: مع تسجيل دخول (Authenticated)
**الهدف:** التأكد من أن الشريط يظهر للمستخدمين المسجلين أيضاً

1. سجل دخول كمستخدم عادي
2. اذهب إلى الواجهة الرئيسية
3. **يجب أن ترى الشريط** ✅

**✅ النجاح:** الشريط يظهر
**❌ الفشل:** الشريط لا يظهر → تحقق من الإعدادات

---

### اختبار 3: زر الإغلاق (X)
**الهدف:** التأكد من أن زر X يعمل

1. افتح الواجهة الرئيسية
2. اضغط على زر X في الشريط
3. **الشريط يجب أن يختفي** ✅
4. حدّث الصفحة (F5)
5. **الشريط يجب أن يظهر مرة أخرى** ✅

**✅ النجاح:** X يخفي الشريط، F5 يعيده
**❌ الفشل:** إذا لم يختفي → مشكلة في JavaScript

---

### اختبار 4: الضغط على الشريط
**الهدف:** التأكد من أن الشريط يفتح صفحة شركاء النجاح

1. افتح الواجهة الرئيسية
2. اضغط على الشريط (في أي مكان عدا X)
3. **يجب أن تفتح صفحة برنامج شركاء النجاح** ✅

**✅ النجاح:** الصفحة تفتح
**❌ الفشل:** لا شيء يحدث → تحقق من onOpenPartnerProgram

---

### اختبار 5: Responsive (الاستجابة)
**الهدف:** التأكد من أن الشريط يظهر بشكل جيد على كل الأجهزة

#### على الجوال 📱:
1. افتح الموقع على جوالك
2. الشريط يجب أن يكون:
   - [ ] واضح وقابل للقراءة
   - [ ] يظهر تحت الهيدر مباشرة
   - [ ] زر X كبير بما يكفي للضغط عليه

#### على التابلت:
1. افتح الموقع على تابلت
2. نفس اختبار الجوال

#### على الكمبيوتر 💻:
1. افتح الموقع على الكمبيوتر
2. الشريط يجب أن يكون:
   - [ ] أكبر قليلاً من الجوال
   - [ ] النص أكبر (lg:text-base)
   - [ ] يظهر بشكل احترافي

**✅ النجاح:** يعمل على كل الأجهزة
**❌ الفشل:** مشاكل في التصميم → تحقق من CSS

---

## 🗄️ اختبار قاعدة البيانات

### الخطوة 1: تحقق من قيمة التفعيل
```sql
SELECT value
FROM system_settings
WHERE key = 'partner_share_message_enabled';
```

**النتيجة المتوقعة:** `'true'`

**إذا كانت النتيجة `'false'`:**
```sql
UPDATE system_settings
SET value = 'true'
WHERE key = 'partner_share_message_enabled';
```

---

### الخطوة 2: تحقق من الـ RLS Policies
```sql
SELECT policyname, roles, cmd
FROM pg_policies
WHERE tablename = 'system_settings';
```

**النتيجة المتوقعة:** 3 policies:
1. "Admins can manage all settings" (roles: authenticated, cmd: ALL)
2. "Users can read public settings" (roles: authenticated, cmd: SELECT)
3. "Anyone can read public and marketing settings" (roles: public, cmd: SELECT) ← **هذه مهمة!**

**إذا كانت Policy 3 غير موجودة:**
```sql
CREATE POLICY "Anyone can read public and marketing settings"
  ON system_settings
  FOR SELECT
  TO public
  USING (category IN ('public', 'marketing'));
```

---

### الخطوة 3: تحقق من الإعدادات الثلاثة
```sql
SELECT key, value, category
FROM system_settings
WHERE key LIKE 'partner_share%';
```

**النتيجة المتوقعة:**
```
key                              | value             | category
---------------------------------|-------------------|----------
partner_share_message_enabled    | true              | marketing
partner_share_message_template   | 🌿 استثمر...      | marketing
partner_share_website_url        | https://ashjari..  | marketing
```

**تحقق من:**
- [ ] `partner_share_message_enabled` = `'true'`
- [ ] `category` = `'marketing'` لكل الثلاثة
- [ ] النص موجود ولا يحتوي على أخطاء

---

## 🖥️ اختبار Console المتصفح

### الخطوة 1: افتح Console (F12)
1. افتح الواجهة الرئيسية
2. اضغط F12 لفتح Developer Tools
3. اذهب إلى تبويب "Console"

### الخطوة 2: ابحث عن الأخطاء
**يجب ألا ترى:**
- ❌ أخطاء متعلقة بـ `system_settings`
- ❌ أخطاء متعلقة بـ `partner_share_message`
- ❌ أخطاء RLS أو permissions

**قد ترى (وهذا طبيعي):**
- ⚠️ تحذيرات عن chunk sizes (هذا لا يؤثر على الوظيفة)

### الخطوة 3: تتبع التحميل
افتح تبويب "Network" وابحث عن:
- `system_settings` query
- يجب أن يكون Status: 200 OK
- يجب أن يُرجع البيانات الثلاثة

---

## ❌ المشاكل الشائعة والحلول

### المشكلة 1: الشريط لا يظهر أبداً
**السبب المحتمل:** RLS policy غير صحيحة أو التفعيل = false

**الحل:**
1. تحقق من قاعدة البيانات (الخطوات أعلاه)
2. تأكد من وجود Policy "Anyone can read public and marketing settings"
3. تأكد من أن `partner_share_message_enabled` = `'true'`

---

### المشكلة 2: الشريط يظهر ثم يختفي سريعاً
**السبب المحتمل:** JavaScript error أو state issue

**الحل:**
1. افتح Console وابحث عن أخطاء
2. تحقق من `partnerBannerEnabled` state في React DevTools
3. تأكد من أن `showPartnerBanner` = true

---

### المشكلة 3: الشريط لا يفتح صفحة الشركاء
**السبب المحتمل:** onOpenPartnerProgram غير موجودة أو غير مربوطة

**الحل:**
1. تحقق من أن `onOpenPartnerProgram` موجودة في props
2. تحقق من أن الـ button له onClick={onOpenPartnerProgram}

---

### المشكلة 4: زر X لا يعمل
**السبب المحتمل:** setShowPartnerBanner غير مربوطة

**الحل:**
1. تحقق من state `showPartnerBanner`
2. تحقق من أن زر X له onClick={() => setShowPartnerBanner(false)}

---

### المشكلة 5: الشريط يظهر على الكمبيوتر فقط (أو الجوال فقط)
**السبب المحتمل:** CSS responsive issue

**الحل:**
1. تحقق من `className="lg:top-16"` على div الشريط
2. تحقق من media queries في CSS

---

## ✅ Checklist النهائي

### قبل البدء:
- [ ] RLS policy "Anyone can read public and marketing settings" موجودة
- [ ] `partner_share_message_enabled` = 'true'
- [ ] Build successful (npm run build)

### اختبارات أساسية:
- [ ] الشريط يظهر بدون تسجيل دخول
- [ ] الشريط يظهر مع تسجيل دخول
- [ ] زر X يعمل
- [ ] الضغط على الشريط يفتح صفحة الشركاء

### اختبارات متقدمة:
- [ ] يعمل على الجوال
- [ ] يعمل على التابلت
- [ ] يعمل على الكمبيوتر
- [ ] لا توجد أخطاء في Console
- [ ] Network request ينجح (200 OK)

---

## 🎯 النتيجة المتوقعة

إذا نجحت في كل الاختبارات:
- ✅ الشريط الذهبي يظهر لجميع المستخدمين
- ✅ التصميم جميل واحترافي
- ✅ التفاعل سلس
- ✅ Responsive ممتاز
- ✅ لا توجد أخطاء
- ✅ جاهز للإنتاج

---

## 📞 دعم

إذا فشل أي اختبار:
1. اتبع خطوات الحل المذكورة في "المشاكل الشائعة"
2. افتح Console وابحث عن الأخطاء
3. تحقق من قاعدة البيانات
4. تحقق من RLS policies
5. امسح الـ Cache (Ctrl+Shift+R)

---

🎊 **اختبار سعيد!**
