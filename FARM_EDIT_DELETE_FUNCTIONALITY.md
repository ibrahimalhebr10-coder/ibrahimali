# تفعيل الحذف والتعديل في بطاقات المزارع

## نظرة عامة

تم تفعيل وظائف الحذف والتعديل بشكل كامل في إدارة المزارع بلوحة التحكم.

---

## الوظائف المفعّلة ✅

### 1. حذف المزرعة 🗑️

**الحالة:** مفعّل ويعمل بالكامل

**كيف يعمل:**
```
1. الأدمن يضغط زر "حذف" على بطاقة المزرعة
2. تظهر نافذة تأكيد الحذف
3. عند التأكيد:
   ✅ يتم حذف المزرعة من قاعدة البيانات
   ✅ يتم تحديث القائمة تلقائياً
   ✅ تختفي المزرعة من الواجهة الرئيسية
```

**الكود:**
```typescript
const handleConfirmDelete = async () => {
  if (!selectedFarm) return;

  setIsDeleting(true);
  try {
    const { error } = await supabase
      .from('farms')
      .delete()
      .eq('id', selectedFarm.id);

    if (error) {
      console.error('Error deleting farm:', error);
      alert('حدث خطأ أثناء حذف المزرعة');
      return;
    }

    setShowDeleteConfirm(false);
    setSelectedFarm(null);
    setRefreshTrigger(prev => prev + 1); // ✅ تحديث القائمة
  } catch (err) {
    console.error('Unexpected error deleting farm:', err);
    alert('حدث خطأ غير متوقع');
  } finally {
    setIsDeleting(false);
  }
};
```

**نافذة التأكيد:**
```
┌─────────────────────────────────────┐
│        🔴 تأكيد الحذف              │
│                                     │
│  هل أنت متأكد من حذف مزرعة          │
│  "اسم المزرعة"؟                    │
│                                     │
│  ⚠️ لا يمكن التراجع عن هذا الإجراء │
│                                     │
│  [إلغاء]      [حذف نهائياً 🗑️]    │
└─────────────────────────────────────┘
```

---

### 2. تعديل المزرعة ✏️

**الحالة:** مفعّل ويعمل بالكامل

**كيف يعمل:**
```
1. الأدمن يضغط زر "تعديل" على بطاقة المزرعة
2. يفتح نموذج التعديل (نفس نموذج الإضافة)
3. النموذج محمّل بالبيانات الحالية للمزرعة
4. عند الحفظ:
   ✅ يتم تحديث المزرعة في قاعدة البيانات
   ✅ يتم تحديث القائمة تلقائياً
   ✅ تظهر التعديلات في الواجهة الرئيسية
```

**التحسينات على نموذج الإضافة:**

#### أ. دعم وضع التعديل

```typescript
interface AddFarmFormProps {
  onClose: () => void;
  onSuccess: () => void;
  editMode?: boolean;  // ✅ جديد!
  farmData?: {         // ✅ جديد!
    id: string;
    name_ar: string;
    location: string;
    category_id: string;
    area_size?: string;
    internal_cost_per_tree?: number;
    total_trees: number;
    coming_soon_label?: string;
    card_description?: string;
    tree_types?: any[];
  };
}
```

#### ب. تحميل البيانات عند التعديل

```typescript
useEffect(() => {
  if (editMode && farmData) {
    // ✅ تحميل جميع البيانات
    setName(farmData.name_ar || '');
    setLocation(farmData.location || '');
    setAreaSize(farmData.area_size || '');
    setInternalCost(farmData.internal_cost_per_tree?.toString() || '');
    setTotalTrees(farmData.total_trees?.toString() || '');
    setComingSoonLabel(farmData.coming_soon_label || 'قريبًا');
    setCardDescription(farmData.card_description || '');
    setSelectedCategory(farmData.category_id || '');

    // ✅ تحميل أنواع الأشجار
    if (farmData.tree_types && farmData.tree_types.length > 0) {
      setTreeTypes(farmData.tree_types.map(t => ({
        name: t.name || '',
        count: t.count || 0
      })));
    }
  }
}, [editMode, farmData]);
```

#### ج. دالة الحفظ المحسّنة

```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setError('');

  if (!validateForm()) return;

  setIsLoading(true);

  try {
    const treeTypesJson = treeTypes.map((tree, index) => ({
      name: tree.name,
      count: tree.count,
      order: index + 1
    }));

    if (editMode && farmData) {
      // ✅ وضع التعديل
      const { error: updateError } = await supabase
        .from('farms')
        .update({
          category_id: selectedCategory,
          name_ar: name,
          name_en: name,
          location: location,
          area_size: areaSize || null,
          internal_cost_per_tree: Number(internalCost) || 0,
          total_trees: Number(totalTrees),
          coming_soon_label: comingSoonLabel || 'قريبًا',
          card_description: cardDescription || null,
          tree_types: treeTypesJson,
          description_ar: cardDescription || name,
          updated_at: new Date().toISOString()
        })
        .eq('id', farmData.id);

      if (updateError) {
        console.error('Update error:', updateError);
        setError('حدث خطأ أثناء تعديل المزرعة');
        return;
      }
    } else {
      // ✅ وضع الإضافة (كما هو)
      // ... كود الإضافة
    }

    onSuccess();
  } catch (err) {
    console.error('Unexpected error:', err);
    setError('حدث خطأ غير متوقع');
  } finally {
    setIsLoading(false);
  }
};
```

#### د. واجهة ديناميكية

**العنوان:**
```typescript
<h2 className="text-2xl font-bold">
  {editMode ? 'تعديل المزرعة' : 'إضافة مزرعة جديدة'}
</h2>
<p className="text-green-100 text-sm mt-1">
  {editMode ? 'تحديث معلومات المزرعة' : 'المزرعة = خزان | التسعير من الباقات'}
</p>
```

**زر الحفظ:**
```typescript
{isLoading ? (
  <>
    <div className="spinner" />
    <span>{editMode ? 'جارٍ التحديث...' : 'جارٍ الحفظ...'}</span>
  </>
) : (
  <>
    <Save className="w-5 h-5" />
    <span>{editMode ? 'تحديث المزرعة' : 'حفظ المزرعة'}</span>
  </>
)}
```

---

### 3. التحديثات في FarmCardsManagement

#### أ. States الجديدة

```typescript
const [showEditForm, setShowEditForm] = useState(false);  // ✅ جديد
```

#### ب. الدوال الجديدة

```typescript
const handleEditClick = (farm: Farm) => {
  setSelectedFarm(farm);
  setShowEditForm(true);
};

const handleEditSuccess = () => {
  setShowEditForm(false);
  setSelectedFarm(null);
  setRefreshTrigger(prev => prev + 1);  // ✅ تحديث القائمة
};
```

#### ج. تحسين Farm Interface

```typescript
interface Farm {
  id: string;
  name_ar: string;
  location: string;
  category_id: string;           // ✅ جديد
  area_size?: string;            // ✅ جديد
  internal_cost_per_tree?: number; // ✅ جديد
  total_trees: number;
  available_trees: number;
  reserved_trees: number;
  card_description: string;
  coming_soon_label: string;
  is_open_for_booking: boolean;
  tree_types: any[];
}
```

#### د. تحسين loadFarms

```typescript
const loadFarms = async () => {
  setIsLoading(true);
  try {
    const { data, error } = await supabase
      .from('farms')
      .select(`
        id,
        name_ar,
        location,
        category_id,              ✅
        area_size,                ✅
        internal_cost_per_tree,   ✅
        total_trees,
        available_trees,
        reserved_trees,
        card_description,
        coming_soon_label,
        is_open_for_booking,
        tree_types
      `)
      .order('created_at', { ascending: false });

    if (error) {
      console.error('Error loading farms:', error);
      return;
    }

    setFarms(data || []);
  } catch (err) {
    console.error('Unexpected error loading farms:', err);
  } finally {
    setIsLoading(false);
  }
};
```

#### هـ. نموذج التعديل في JSX

```typescript
{/* Edit Farm Modal */}
{showEditForm && selectedFarm && (
  <AddFarmForm
    onClose={() => {
      setShowEditForm(false);
      setSelectedFarm(null);
    }}
    onSuccess={handleEditSuccess}
    editMode={true}  // ✅ وضع التعديل
    farmData={{
      id: selectedFarm.id,
      name_ar: selectedFarm.name_ar,
      location: selectedFarm.location,
      category_id: selectedFarm.category_id,
      area_size: selectedFarm.area_size,
      internal_cost_per_tree: selectedFarm.internal_cost_per_tree,
      total_trees: selectedFarm.total_trees,
      coming_soon_label: selectedFarm.coming_soon_label,
      card_description: selectedFarm.card_description,
      tree_types: selectedFarm.tree_types
    }}
  />
)}
```

#### و. أزرار التعديل المفعّلة

**في البطاقات:**
```typescript
<button
  onClick={() => handleEditClick(farm)}  // ✅ مفعّل
  className="..."
>
  <Edit2 className="w-4 h-4" />
  <span className="text-xs font-semibold">تعديل</span>
</button>
```

**في نافذة التفاصيل:**
```typescript
<button
  onClick={() => {
    setShowDetailsModal(false);
    handleEditClick(selectedFarm!);  // ✅ مفعّل
  }}
  className="..."
>
  تعديل المزرعة
</button>
```

---

## التدفق الكامل

### سيناريو 1: حذف مزرعة

```
┌─────────────────────┐
│  الأدمن في لوحة    │
│     التحكم          │
└──────────┬──────────┘
           │
           │ 1. يضغط "حذف" على مزرعة
           ▼
┌─────────────────────┐
│  نافذة تأكيد الحذف  │
│                     │
│  هل أنت متأكد؟     │
│  [إلغاء] [حذف]     │
└──────────┬──────────┘
           │
           │ 2. يؤكد الحذف
           ▼
┌─────────────────────┐
│  DELETE FROM farms  │
│  WHERE id = 'xxx'   │
└──────────┬──────────┘
           │
           │ 3. نجح الحذف
           ▼
┌─────────────────────┐
│  تحديث القائمة      │
│  refreshTrigger++   │
└──────────┬──────────┘
           │
           │ 4. إعادة تحميل المزارع
           ▼
┌─────────────────────┐
│  المزرعة اختفت     │
│  من القائمة         │
└─────────────────────┘
```

### سيناريو 2: تعديل مزرعة

```
┌─────────────────────┐
│  الأدمن في لوحة    │
│     التحكم          │
└──────────┬──────────┘
           │
           │ 1. يضغط "تعديل" على مزرعة
           ▼
┌─────────────────────┐
│  نموذج التعديل      │
│  (محمّل بالبيانات)  │
│                     │
│  الاسم: مزرعة XXX  │
│  الموقع: القصيم     │
│  الفئة: النخيل      │
│  ...                │
└──────────┬──────────┘
           │
           │ 2. يعدّل البيانات
           │ 3. يضغط "تحديث المزرعة"
           ▼
┌─────────────────────┐
│  UPDATE farms       │
│  SET ...            │
│  WHERE id = 'xxx'   │
└──────────┬──────────┘
           │
           │ 4. نجح التحديث
           ▼
┌─────────────────────┐
│  تحديث القائمة      │
│  refreshTrigger++   │
└──────────┬──────────┘
           │
           │ 5. إعادة تحميل المزارع
           ▼
┌─────────────────────┐
│  البيانات المحدثة   │
│  تظهر في البطاقة    │
└─────────────────────┘
```

---

## الأمان (RLS Policies)

### حذف المزرعة

**السياسة:**
```sql
CREATE POLICY "Admins can delete farms"
  ON farms FOR DELETE
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM admins
      WHERE admins.user_id = auth.uid()
      AND admins.is_active = true
    )
  );
```

**التحقق:**
- ✅ يجب أن يكون المستخدم مسجل دخول
- ✅ يجب أن يكون في جدول admins
- ✅ يجب أن يكون is_active = true

### تعديل المزرعة

**السياسة:**
```sql
CREATE POLICY "Admins can update farms"
  ON farms FOR UPDATE
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM admins
      WHERE admins.user_id = auth.uid()
      AND admins.is_active = true
    )
  )
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM admins
      WHERE admins.user_id = auth.uid()
      AND admins.is_active = true
    )
  );
```

**التحقق:**
- ✅ USING: للقراءة - من يستطيع تعديل المزرعة؟
- ✅ WITH CHECK: للكتابة - هل البيانات الجديدة صالحة؟

---

## اختبار الوظائف

### اختبار الحذف

**الخطوات:**
```
1. افتح لوحة التحكم
2. انتقل إلى "إدارة المزارع"
3. اختر أي مزرعة
4. اضغط زر "حذف"
5. أكد الحذف في النافذة
```

**النتيجة المتوقعة:**
```
✅ تظهر نافذة التأكيد
✅ عند التأكيد، تظهر رسالة "جارٍ الحذف..."
✅ المزرعة تختفي من القائمة فوراً
✅ عند فتح الواجهة الرئيسية، المزرعة غير موجودة
```

### اختبار التعديل

**الخطوات:**
```
1. افتح لوحة التحكم
2. انتقل إلى "إدارة المزارع"
3. اختر أي مزرعة
4. اضغط زر "تعديل"
5. عدّل البيانات:
   - الاسم: من "مزرعة A" إلى "مزرعة B المحدثة"
   - الموقع: من "القصيم" إلى "الرياض"
   - الفئة: من "النخيل" إلى "الزيتون"
   - عدد الأشجار: من 100 إلى 150
6. اضغط "تحديث المزرعة"
```

**النتيجة المتوقعة:**
```
✅ يفتح نموذج التعديل محمّل بالبيانات الحالية
✅ جميع الحقول قابلة للتعديل
✅ عند الحفظ، تظهر رسالة "جارٍ التحديث..."
✅ النموذج يُغلق تلقائياً
✅ البطاقة تتحدث بالبيانات الجديدة فوراً
✅ في الواجهة الرئيسية:
   - الاسم الجديد يظهر
   - تحت فئة "الزيتون" (الفئة الجديدة)
   - مع العدد الجديد 150
```

---

## معالجة الأخطاء

### عند الحذف

**إذا فشل الحذف:**
```typescript
if (error) {
  console.error('Error deleting farm:', error);
  alert('حدث خطأ أثناء حذف المزرعة');
  return;
}
```

**الحالات:**
1. مشكلة في الشبكة → alert
2. عدم صلاحية → error في console
3. خطأ في RLS → error في console

### عند التعديل

**إذا فشل التعديل:**
```typescript
if (updateError) {
  console.error('Update error:', updateError);
  setError('حدث خطأ أثناء تعديل المزرعة');
  return;
}
```

**الحالات:**
1. بيانات غير صحيحة → رسالة خطأ في النموذج
2. مشكلة في الشبكة → رسالة خطأ
3. عدم صلاحية → error في console

---

## الملفات المعدلة

### 1. AddFarmForm.tsx

**التعديلات:**
```typescript
✅ إضافة editMode و farmData في Props
✅ useEffect لتحميل البيانات عند التعديل
✅ تحسين handleSubmit لدعم UPDATE
✅ عنوان ديناميكي حسب الوضع
✅ نص زر ديناميكي حسب الوضع
```

**الأسطر المعدلة:**
- السطر 16-31: Props مع editMode وfarmData
- السطر 40-60: useEffect لتحميل البيانات
- السطر 105-170: handleSubmit مع UPDATE
- السطر 188-196: عنوان ديناميكي
- السطر 469-479: زر ديناميكي

### 2. FarmCardsManagement.tsx

**التعديلات:**
```typescript
✅ إضافة showEditForm state
✅ إضافة handleEditClick و handleEditSuccess
✅ تحسين Farm interface
✅ تحسين loadFarms لجلب حقول إضافية
✅ تفعيل أزرار التعديل
✅ إضافة نموذج التعديل في JSX
```

**الأسطر المعدلة:**
- السطر 6-17: Farm interface محسّن
- السطر 23: showEditForm state
- السطر 54-62: دوال التعديل الجديدة
- السطر 36-52: loadFarms محسّن
- السطر 293: زر تعديل في البطاقة
- السطر 444-450: زر تعديل في نافذة التفاصيل
- السطر 317-338: نموذج التعديل

---

## التحديث التلقائي

### في لوحة التحكم

**بعد الحذف:**
```typescript
setRefreshTrigger(prev => prev + 1);
```

**بعد التعديل:**
```typescript
setRefreshTrigger(prev => prev + 1);
```

**useEffect يراقب refreshTrigger:**
```typescript
useEffect(() => {
  loadFarms();  // ✅ إعادة تحميل المزارع
}, [refreshTrigger]);
```

### في الواجهة الرئيسية

**التحديث التلقائي:**
- عند فتح التطبيق → تحميل المزارع
- عند تغيير الفئة → عرض المزارع المناسبة
- البيانات المحذوفة/المعدلة تنعكس مباشرة

**لإعادة التحميل يدوياً:**
```
تحديث الصفحة (F5)
```

---

## الخلاصة

### ما تم إنجازه ✅

1. **الحذف:**
   - ✅ نافذة تأكيد احترافية
   - ✅ حذف آمن من قاعدة البيانات
   - ✅ تحديث تلقائي للقائمة
   - ✅ حماية بـ RLS policies

2. **التعديل:**
   - ✅ نموذج تعديل كامل الوظائف
   - ✅ تحميل تلقائي للبيانات الحالية
   - ✅ تحديث آمن في قاعدة البيانات
   - ✅ واجهة ديناميكية حسب الوضع
   - ✅ حماية بـ RLS policies

3. **التحديث التلقائي:**
   - ✅ في لوحة التحكم بعد أي عملية
   - ✅ في الواجهة الرئيسية عند الفتح

4. **معالجة الأخطاء:**
   - ✅ رسائل خطأ واضحة
   - ✅ تسجيل في console للتتبع
   - ✅ منع العمليات الخاطئة

### الحالة النهائية

```
🟢 الحذف: يعمل 100%
🟢 التعديل: يعمل 100%
🟢 Build: ناجح ✅
🟢 التحديث التلقائي: مفعّل ✅
🟢 الأمان: RLS policies محكمة ✅
```

---

**تاريخ الإكمال:** 2026-02-02
**Build:** ناجح بدون أخطاء ✅
**الوظائف:** حذف + تعديل مفعّلة بالكامل ✅
