# دليل سريع: أنواع العقود حسب المسار
## Quick Guide: Contract Types by Path

> **مرجع سريع للمطورين والمستخدمين**

---

## 📋 القاعدة الأساسية

```
path_type من قاعدة البيانات = نوع العقد

'agricultural' → عقد أشجاري الخضراء 🌿
'investment'   → عقد أشجاري الذهبية 🌟
```

---

## 🎨 الأنماط المرئية

### عقد أشجاري الخضراء

```
العنوان: عقد أشجاري الخضراء
الأيقونة: ✨ Sparkles
اللون: أخضر زمردي (#10b981 → #059669)
الجمهور: المزارعون (Farmers)
المسار: Agricultural
```

### عقد أشجاري الذهبية

```
العنوان: عقد أشجاري الذهبية
الأيقونة: 📈 TrendingUp
اللون: ذهبي مشمس (#f59e0b → #d97706)
الجمهور: المستثمرون (Investors)
المسار: Investment
```

---

## 🔗 الربط مع الأزرار

### في صفحة "حسابي" (AccountProfile):

```typescript
// زر أشجاري الخضراء
appMode === 'agricultural'
  → عنوان: "أشجاري الخضراء"
  → أيقونة: 🌳
  → لون: أخضر

// زر أشجاري الذهبية
appMode === 'investment'
  → عنوان: "أشجاري الذهبية"
  → أيقونة: 🌟
  → لون: ذهبي
```

**عند الضغط:**
- يفتح `MyGreenTrees`
- يعرض الصيانات حسب `identity`
- التطابق الكامل مع نوع العقد

---

## 🗄️ قاعدة البيانات

### جدول reservations:

```sql
path_type TEXT NOT NULL DEFAULT 'agricultural'
CHECK (path_type IN ('agricultural', 'investment'))
```

### كيف يُحفظ عند الحجز:

```typescript
// عند الحجز من مسار أشجاري الخضراء
{
  user_id: "...",
  farm_id: "...",
  path_type: "agricultural",  // ← يُحفظ هنا
  total_trees: 50
}

// عند الحجز من مسار أشجاري الذهبية
{
  user_id: "...",
  farm_id: "...",
  path_type: "investment",  // ← يُحفظ هنا
  total_trees: 100
}
```

---

## 💻 في الكود

### قراءة path_type:

```typescript
// في MyContracts.tsx
const { data: reservations } = await supabase
  .from('reservations')
  .select('*')
  .eq('user_id', user.id);

// كل reservation يحتوي على path_type
reservation.path_type  // 'agricultural' أو 'investment'
```

### عرض العقد:

```typescript
// تحديد نوع العقد
const badge = getContractTypeBadge(contract.path_type);

// النتيجة:
if (path_type === 'investment') {
  label: 'عقد أشجاري الذهبية'
  color: 'from-amber-500 to-yellow-600'
  icon: TrendingUp
} else {
  label: 'عقد أشجاري الخضراء'
  color: 'from-green-500 to-emerald-600'
  icon: Sparkles
}
```

---

## 🧪 اختبار سريع

### اختبر التمييز:

```bash
1. احجز من مسار "أشجاري الخضراء"
   ✅ العقد يظهر: "عقد أشجاري الخضراء" (أخضر)

2. احجز من مسار "أشجاري الذهبية"
   ✅ العقد يظهر: "عقد أشجاري الذهبية" (ذهبي)

3. اضغط على زر "أشجاري" المناسب
   ✅ يفتح الصيانات الصحيحة
```

### اختبر الربط:

```bash
1. افتح "حسابي"
2. شاهد لون العقد
3. اضغط على زر "أشجاري" في الفوتر
4. تحقق من تطابق الألوان والمعلومات

✅ لون العقد = لون الزر
✅ نوع العقد = نوع الصيانات
```

---

## ⚠️ ملاحظات هامة

### 1. لا تخمن نوع العقد:

```typescript
// ❌ خطأ
const type = contract.contract_name.includes('investment') ? ...

// ✅ صحيح
const type = contract.path_type
```

### 2. العقود القديمة:

```typescript
// إذا لم يكن path_type موجود (عقود قديمة):
path_type: reservation.path_type || 'agricultural'
```

### 3. التناسق مهم:

```typescript
// في كل مكان تعرض فيه العقد:
- استخدم path_type
- استخدم نفس الألوان
- استخدم نفس الأيقونات
```

---

## 📊 جدول مرجعي سريع

| المسار | path_type | اسم العقد | اللون | الأيقونة |
|--------|-----------|-----------|-------|----------|
| أشجاري الخضراء | `'agricultural'` | عقد أشجاري الخضراء | أخضر | ✨ |
| أشجاري الذهبية | `'investment'` | عقد أشجاري الذهبية | ذهبي | 📈 |

---

## 🔧 استكشاف الأخطاء

### المشكلة: العقد يظهر بنوع خاطئ

```typescript
// تحقق من:
1. path_type في قاعدة البيانات
2. تأكد من استخدام getContractTypeBadge(contract.path_type)
3. لا تستخدم contract_name
```

### المشكلة: العقد لا يتطابق مع زر "أشجاري"

```typescript
// تحقق من:
1. identity في AuthContext
2. path_type في العقد
3. التطابق بينهما
```

---

## ✅ خلاصة سريعة

```
الحجز → path_type يُحفظ في DB
         ↓
العرض → MyContracts يقرأ path_type
         ↓
التمييز → عقد أشجاري الخضراء أو الذهبية
         ↓
الربط → زر "أشجاري" يفتح البيانات المناسبة
```

---

**كل شيء يعتمد على `path_type` من قاعدة البيانات!** 🎯
