# كيف تختبر جمع السنوات المجانية 🧪

دليل سريع لاختبار أن السنوات المجانية تُجمع بشكل صحيح

---

## اختبار سريع (30 ثانية) ⚡

```
1. افتح أي مزرعة
   
2. أدخل كود: HNHTBH7780
   
3. اضغط ✓
   
4. يجب أن ترى: "مبروك! حصلت على +3 سنوات مجاناً"
   
5. اختر باقة (مثلاً: 50 شجرة)
   
6. انظر إلى "مدة إضافية مجانية" في ملخص الحجز
   
7. يجب أن ترى المجموع:
   ✅ (سنوات من الكود + سنوات من الباقة)
   ✅ وليس واحدة فقط
```

---

## مثال عملي 🎯

### السيناريو:

```
كود الشريك يعطي: +3 سنوات
الباقة (50 شجرة) تعطي: عقد 3 سنوات + 1 سنة مجانية
```

### ما يجب أن تراه:

```
ملخص الحجز:
├── عدد الأشجار: 50 شجرة
├── مدة العقد: 3 سنوات
└── مدة إضافية مجانية: +4 سنوات ✅
                         ↑
                  (1 + 3 = 4)
```

### إذا رأيت +1 أو +3 فقط:

```
❌ خطأ! يجب أن يكون +4
❌ المشكلة: لم يتم الجمع بشكل صحيح
```

---

## حالات الاختبار 📋

### الحالة 1: مع كود + مع باقة

```
الخطوات:
1. أدخل كود شريك ✓
2. اختر "مع باقة"
3. اختر باقة لها سنوات مجانية

النتيجة المتوقعة:
✅ مدة إضافية = (من الكود + من الباقة)
```

### الحالة 2: مع كود + بدون باقة

```
الخطوات:
1. أدخل كود شريك ✓
2. اختر "بدون باقة" (في المسار الزراعي فقط)

النتيجة المتوقعة:
✅ مدة إضافية = (من الكود + من العقد الأساسي)
```

### الحالة 3: بدون كود + مع باقة

```
الخطوات:
1. لا تدخل كود
2. اختر باقة لها سنوات مجانية

النتيجة المتوقعة:
✅ مدة إضافية = (من الباقة فقط)
```

### الحالة 4: بدون كود + بدون باقة

```
الخطوات:
1. لا تدخل كود
2. اختر "بدون باقة"

النتيجة المتوقعة:
- إذا العقد له سنوات مجانية: تظهر
- إذا لا: لا تظهر خانة "مدة إضافية"
```

---

## جدول الاختبار الكامل ✅

| كود؟ | باقة؟ | مدة عقد | مجاني باقة | مجاني كود | المجموع المتوقع |
|------|-------|---------|-------------|-----------|-----------------|
| ✅ | ✅ | 3 | 1 | 3 | +4 سنوات |
| ✅ | ❌ | 3 | 0 | 3 | +3 سنوات |
| ❌ | ✅ | 3 | 1 | 0 | +1 سنة |
| ❌ | ❌ | 3 | 0 | 0 | لا تظهر |

---

## نصائح الاختبار 💡

```
✅ اختبر على المسار الزراعي والاستثماري
✅ اختبر مع أكواد مختلفة
✅ اختبر مع باقات مختلفة
✅ تحقق من ملخص الحجز قبل الحجز
✅ افتح Console (F12) للتحقق من الأخطاء
```

---

## إذا كان هناك خطأ 🔍

### الأعراض:

```
❌ المجموع خاطئ
❌ يظهر رقم واحد فقط
❌ لا تظهر السنوات من الكود
❌ لا تظهر السنوات من الباقة
```

### التحقق:

```javascript
// افتح Console (F12) واكتب:
const bonusFromCode = 3;
const bonusFromPackage = 1;
const total = bonusFromCode + bonusFromPackage;
console.log('المجموع يجب أن يكون:', total);  // 4
```

### التحقق من قاعدة البيانات:

```sql
-- تحقق من الباقات الزراعية
SELECT package_name, bonus_years 
FROM agricultural_packages 
WHERE is_active = true;

-- تحقق من الباقات الاستثمارية
SELECT package_name, bonus_free_years 
FROM investment_packages 
WHERE is_active = true;
```

---

## مثال تفاعلي كامل 🎮

```
[فتح المنصة]
         ↓
[اختيار مزرعة زراعية]
         ↓
[إدخال كود: HNHTBH7780]
         ↓
[الضغط على ✓]
         ↓
[يظهر: "مبروك! حصلت على +3 سنوات مجاناً"]
         ↓
[اختيار "مع باقة"]
         ↓
[اختيار باقة 50 شجرة]
  الباقة تعطي:
  - عقد 3 سنوات
  - +1 سنة مجانية
         ↓
[النظر إلى ملخص الحجز]
         ↓
[التحقق من "مدة إضافية مجانية"]
         ↓
[يجب أن يكون: +4 سنوات]
  ✅ (1 من الباقة + 3 من الكود = 4)
         ↓
[إذا كان +4 ← الاختبار نجح! 🎉]
[إذا كان +1 أو +3 فقط ← الاختبار فشل! ❌]
```

---

## نماذج اختبار 📝

### نموذج 1: زراعي مع كود

```
✓ المزرعة: أي مزرعة زراعية
✓ الكود: HNHTBH7780
✓ الباقة: 50 شجرة (عقد 3 سنوات + 1 مجاني)
✓ المتوقع: +4 سنوات مجانية
✓ الفعلي: ___ سنوات
✓ النتيجة: نجح ☐  فشل ☐
```

### نموذج 2: استثماري مع كود

```
✓ المزرعة: أي مزرعة استثمارية
✓ الكود: HNHTBH7780
✓ الباقة: 100 شجرة (عقد 5 سنوات + 2 مجاني)
✓ المتوقع: +5 سنوات مجانية
✓ الفعلي: ___ سنوات
✓ النتيجة: نجح ☐  فشل ☐
```

### نموذج 3: زراعي بدون كود

```
✓ المزرعة: أي مزرعة زراعية
✓ الكود: بدون
✓ الباقة: 30 شجرة (عقد 2 سنوات + 1 مجاني)
✓ المتوقع: +1 سنة مجانية
✓ الفعلي: ___ سنوات
✓ النتيجة: نجح ☐  فشل ☐
```

---

**اختبار سعيد! 🎉**
