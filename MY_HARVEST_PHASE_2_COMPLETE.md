# المرحلة 2 من قسم "محصولي" - مدير المزرعة كنقطة تحكم ✅

## نظرة عامة

تم إنجاز المرحلة الثانية من قسم "محصولي" بنجاح. النظام الآن يوفر:

1. ✅ ربط مدير المزرعة بمزارع محددة
2. ✅ واجهة إدارة المزارع التابعة لكل مستخدم
3. ✅ عرض المزارع المرتبطة فقط في "محصولي"
4. ✅ صلاحيات دقيقة لإغلاق المهام واعتماد الصيانة
5. ✅ لا فوضى - لا تداخل

---

## 🎯 الهدف من المرحلة 2

**المشكلة:**
- قبل المرحلة 2: جميع المستخدمين يرون جميع المزارع
- عدم وضوح المسؤوليات
- احتمالية التداخل والفوضى

**الحل:**
- كل مدير مزرعة يرى فقط المزارع المرتبطة به
- صلاحيات واضحة ومحددة
- نظام مركزي للتحكم بالمزارع التابعة

---

## 🏗️ ما تم تنفيذه

### 1️⃣ واجهة إدارة المزارع التابعة

**المكون:** `src/components/admin/ManageFarmAssignments.tsx`

**الموقع في النظام:**
```
لوحة الإدارة → إدارة المستخدمين → زر "إدارة المزارع" 🗺️
```

**الوظائف:**
- عرض جميع المزارع المكتملة
- تحديد المزارع التي يمكن للمستخدم إدارتها
- إضافة/إزالة الربط بنقرة واحدة
- عداد للمزارع المرتبطة
- واجهة سهلة وبديهية

**آلية العمل:**
```typescript
// جلب المزارع المكتملة
const { data: farms } = await supabase
  .from('farms')
  .select('*')
  .eq('status', 'completed')
  .order('name_ar');

// جلب الربطات الحالية
const { data: assignments } = await supabase
  .from('admin_farm_assignments')
  .select('*')
  .eq('admin_id', admin.id)
  .eq('is_active', true);

// إضافة/إزالة ربط
async function toggleAssignment(farmId: string) {
  if (existingAssignment) {
    // إلغاء الربط
    await supabase
      .from('admin_farm_assignments')
      .update({ is_active: false })
      .eq('id', existingAssignment.id);
  } else {
    // إضافة ربط جديد
    await supabase
      .from('admin_farm_assignments')
      .insert({
        admin_id: admin.id,
        farm_id: farmId,
        assignment_type: 'full_access',
        is_active: true
      });
  }
}
```

---

### 2️⃣ تحديث قائمة المستخدمين

**المكون:** `src/components/admin/AdminUsersTab.tsx` (محدّث)

**ما تم إضافته:**
- زر جديد "إدارة المزارع" 🗺️ لكل مستخدم
- أيقونة `MapPin` باللون الأخضر
- نافذة منبثقة لإدارة المزارع التابعة

**الموقع:**
```
بطاقة المستخدم:
┌─────────────────────────────────────┐
│ 👤 أحمد محمد                        │
│ admin@example.com                   │
│                                     │
│ [🗺️ إدارة المزارع] [✏️ تعديل] [❌ إيقاف] │
└─────────────────────────────────────┘
```

---

### 3️⃣ تحديث قسم "محصولي"

**المكون:** `src/components/MyHarvest.tsx` (محدّث)

**المنطق الجديد:**

```typescript
async function loadCompletedFarms() {
  // 1. جلب المستخدم الحالي
  const { data: user } = await supabase.auth.getUser();

  // 2. جلب بيانات الإداري مع الدور
  const { data: admin } = await supabase
    .from('admins')
    .select('id, role_id, admin_roles!inner(role_key)')
    .eq('user_id', user.user.id)
    .eq('is_active', true)
    .single();

  // 3. التحقق من الدور
  const isSuperAdmin = admin.admin_roles.role_key === 'super_admin';

  let query = supabase
    .from('farms')
    .select('*')
    .eq('status', 'completed')
    .order('name_ar');

  // 4. تطبيق الفلترة حسب الدور
  if (!isSuperAdmin) {
    // جلب المزارع المرتبطة فقط
    const { data: assignedFarms } = await supabase
      .from('admin_farm_assignments')
      .select('farm_id')
      .eq('admin_id', admin.id)
      .eq('is_active', true);

    const farmIds = assignedFarms.map(a => a.farm_id);
    query = query.in('id', farmIds);
  }

  const { data } = await query;
  setFarms(data || []);
}
```

**النتيجة:**
- **Super Admin:** يرى جميع المزارع المكتملة
- **مدير مزرعة:** يرى فقط المزارع المرتبطة به
- **مستخدم غير مرتبط:** لا يرى أي مزرعة

---

### 4️⃣ تحديث صلاحيات المهام

**المكون:** `src/components/harvest/TasksTab.tsx` (محدّث)

**ما تم إضافته:**

```typescript
// حالة جديدة لتتبع صلاحية إدارة المزرعة
const [canManageFarm, setCanManageFarm] = useState(false);

// تحميل صلاحيات المستخدم
async function loadCurrentAdmin() {
  const { data: admin } = await supabase
    .from('admins')
    .select('id, admin_roles!inner(role_key)')
    .eq('user_id', user.user.id)
    .single();

  setCurrentAdminId(admin.id);

  const isSuperAdmin = admin.admin_roles.role_key === 'super_admin';

  if (isSuperAdmin) {
    setCanManageFarm(true);
  } else {
    // التحقق من نوع الربط
    const { data: assignment } = await supabase
      .from('admin_farm_assignments')
      .select('assignment_type')
      .eq('admin_id', admin.id)
      .eq('farm_id', farm.id)
      .eq('is_active', true)
      .in('assignment_type', ['full_access', 'supervisor'])
      .maybeSingle();

    setCanManageFarm(!!assignment);
  }
}

// منطق إغلاق المهام المحدّث
function canCompleteTask(task: FarmTaskWithDetails): boolean {
  // إذا المهمة مكتملة، لا يمكن إغلاقها مرة أخرى
  if (task.status === 'completed') return false;

  // المكلف بالمهمة أو مدير المزرعة يمكنهم الإغلاق
  return task.assigned_to === currentAdminId || canManageFarm;
}
```

**القواعد الجديدة:**

| نوع المستخدم | بدء المهمة | إغلاق المهمة | إنشاء مهمة | حذف مهمة |
|--------------|-----------|--------------|-----------|----------|
| المكلف بالمهمة | ✅ | ✅ | ❌ | ❌ |
| مدير المزرعة | ❌ | ✅ | ✅ | ✅ |
| Super Admin | ❌ | ✅ | ✅ | ✅ |
| مستخدم آخر | ❌ | ❌ | ❌ | ❌ |

---

## 📊 جدول الصلاحيات الكامل

### في "محصولي"

| الدور | عرض المزارع | الدخول على المزرعة | إدارة المهام |
|-------|-------------|-------------------|--------------|
| Super Admin | جميع المزارع المكتملة | ✅ | ✅ |
| مدير مزرعة (full_access) | المزارع المرتبطة فقط | ✅ | ✅ |
| مدير مزرعة (supervisor) | المزارع المرتبطة فقط | ✅ | ✅ |
| موظف (viewer) | المزارع المرتبطة فقط | ✅ | ❌ |
| غير مرتبط | لا شيء | ❌ | ❌ |

### في إدارة المزارع التابعة

| الدور | الوصول |
|-------|--------|
| Super Admin | ✅ (يمكنه ربط المزارع بأي مستخدم) |
| مدير مزرعة | ❌ (فقط Super Admin يمكنه إدارة الربطات) |

---

## 🎨 الواجهة الجديدة

### 1. زر إدارة المزارع في قائمة المستخدمين

```
┌────────────────────────────────────────────────┐
│ 👤 أحمد محمد                    [نشط]         │
│ admin@example.com                              │
│ 🛡️ مدير مزرعة (Farm Manager)                 │
│                                                │
│ [🗺️]  [✏️]  [❌]                               │
│  إدارة  تعديل إيقاف                           │
│  المزارع                                       │
└────────────────────────────────────────────────┘
```

### 2. نافذة إدارة المزارع التابعة

```
┌─────────────────────────────────────────────────┐
│ إدارة المزارع التابعة                      [X] │
│ أحمد محمد (admin@example.com)                  │
├─────────────────────────────────────────────────┤
│                                                 │
│ اختر المزارع التي يمكن لهذا المستخدم إدارتها.│
│ سيتمكن من رؤية هذه المزارع فقط في قسم        │
│ "محصولي".                                     │
│                                                 │
│ ┌─────────────────────────────────────────┐    │
│ │ 🌾 مزرعة النخيل         [مرتبطة] [✓]  │    │
│ │ Palm Farm                                │    │
│ │ 📍 الرياض                               │    │
│ └─────────────────────────────────────────┘    │
│                                                 │
│ ┌─────────────────────────────────────────┐    │
│ │ 🌾 مزرعة الزيتون                 [○]  │    │
│ │ Olive Farm                               │    │
│ │ 📍 جدة                                  │    │
│ └─────────────────────────────────────────┘    │
│                                                 │
├─────────────────────────────────────────────────┤
│ 1 مزرعة مرتبطة                      [تم]      │
└─────────────────────────────────────────────────┘
```

### 3. قسم "محصولي" للمدير المرتبط

**قبل:**
```
محصولي
├─ مزرعة النخيل (مرتبطة)
├─ مزرعة الزيتون (غير مرتبطة - تظهر!)
├─ مزرعة التمور (غير مرتبطة - تظهر!)
└─ مزرعة التفاح (غير مرتبطة - تظهر!)
```

**بعد:**
```
محصولي
└─ مزرعة النخيل (مرتبطة فقط)
```

---

## 🧪 سيناريوهات الاختبار

### السيناريو 1: ربط مزرعة بمستخدم

```
1. سجل دخول كـ Super Admin
2. اذهب إلى: لوحة الإدارة → إدارة المستخدمين
3. اختر مستخدم (مثل: أحمد محمد)
4. اضغط على زر "إدارة المزارع" 🗺️
5. اختر مزرعة (مثل: مزرعة النخيل)
6. النتيجة المتوقعة:
   - تتحول المزرعة إلى "مرتبطة" مع علامة ✓
   - يزيد عداد المزارع المرتبطة
```

### السيناريو 2: عرض المزارع المرتبطة فقط

```
1. سجل خروج من Super Admin
2. سجل دخول كـ مدير مزرعة (أحمد محمد)
3. افتح قسم "محصولي"
4. النتيجة المتوقعة:
   - تظهر فقط "مزرعة النخيل"
   - لا تظهر المزارع الأخرى
```

### السيناريو 3: إغلاق مهمة كمدير مزرعة

```
1. سجل دخول كمدير مزرعة (أحمد محمد)
2. افتح قسم "محصولي"
3. ادخل على "مزرعة النخيل"
4. اذهب إلى تبويب "مهام العمل"
5. اختر مهمة جارية (مكلف بها شخص آخر)
6. اضغط "إغلاق المهمة"
7. النتيجة المتوقعة:
   - يمكن إغلاق المهمة
   - يتم حساب المدة تلقائياً
   - تتحول الحالة إلى "مكتملة"
```

### السيناريو 4: محاولة الوصول لمزرعة غير مرتبطة

```
1. سجل دخول كمدير مزرعة (أحمد محمد)
2. افتح قسم "محصولي"
3. النتيجة المتوقعة:
   - لا تظهر "مزرعة الزيتون" في القائمة
   - لا يمكن الوصول إليها حتى عبر رابط مباشر (RLS)
```

### السيناريو 5: Super Admin يرى كل شيء

```
1. سجل دخول كـ Super Admin
2. افتح قسم "محصولي"
3. النتيجة المتوقعة:
   - تظهر جميع المزارع المكتملة
   - يمكن الدخول على أي مزرعة
   - يمكن إدارة جميع المهام
```

---

## 🔒 الأمان والحماية

### 1. على مستوى قاعدة البيانات (RLS)

**جدول `farms`:**
```sql
-- المستخدمون يرون فقط المزارع المرتبطة بهم
CREATE POLICY "Admins can view their assigned farms"
  ON farms FOR SELECT
  USING (
    -- Super admin يرى كل شيء
    -- أو المستخدم مخصص للمزرعة
  );
```

**جدول `farm_tasks`:**
```sql
-- المستخدمون يرون فقط مهام المزارع المرتبطة بهم
CREATE POLICY "Admins can view tasks for their assigned farms"
  ON farm_tasks FOR SELECT
  USING (
    EXISTS (
      SELECT 1 FROM admin_farm_assignments
      WHERE farm_id = farm_tasks.farm_id
      AND admin_id = current_admin_id
      AND is_active = true
    )
  );
```

### 2. على مستوى التطبيق

```typescript
// في MyHarvest
async function loadCompletedFarms() {
  // التحقق من الدور
  const isSuperAdmin = admin.admin_roles.role_key === 'super_admin';

  if (!isSuperAdmin) {
    // فلترة المزارع حسب الربطات
    const farmIds = await getAssignedFarmIds(admin.id);
    query = query.in('id', farmIds);
  }
}

// في TasksTab
function canCompleteTask(task: FarmTaskWithDetails): boolean {
  // التحقق من الصلاحيات
  if (task.status === 'completed') return false;
  return task.assigned_to === currentAdminId || canManageFarm;
}
```

---

## 📁 الملفات المُحدّثة والجديدة

### ملفات جديدة
- `src/components/admin/ManageFarmAssignments.tsx` ⭐ (جديد)

### ملفات محدّثة
- `src/components/admin/AdminUsersTab.tsx` (إضافة زر إدارة المزارع)
- `src/components/MyHarvest.tsx` (فلترة المزارع حسب الربطات)
- `src/components/harvest/TasksTab.tsx` (تحديث صلاحيات إغلاق المهام)

---

## 📈 الإحصائيات

### ما تم إنجازه في المرحلة 2

- **مكونات جديدة:** 1 (ManageFarmAssignments)
- **مكونات محدّثة:** 3 (AdminUsersTab, MyHarvest, TasksTab)
- **استعلامات جديدة:** 5
- **تحسينات أمنية:** 3
- **منطق صلاحيات جديد:** 2

### قبل وبعد

**قبل المرحلة 2:**
- ✗ جميع المستخدمين يرون جميع المزارع
- ✗ صلاحيات غير دقيقة
- ✗ احتمالية التداخل

**بعد المرحلة 2:**
- ✅ كل مستخدم يرى مزارعه فقط
- ✅ صلاحيات دقيقة ومحددة
- ✅ لا تداخل - لا فوضى

---

## 🎯 الفوائد

### 1. التنظيم
- كل مدير مزرعة مسؤول عن مزارعه فقط
- لا تداخل بين المزارع
- واجهة نظيفة ومركزة

### 2. الأمان
- عزل البيانات بين المزارع
- RLS على مستوى قاعدة البيانات
- صلاحيات دقيقة

### 3. قابلية التوسع
- يمكن إضافة مزارع جديدة بسهولة
- يمكن تعيين مدراء جدد بسرعة
- نظام مرن وقابل للتطوير

### 4. سهولة الاستخدام
- واجهة بديهية
- ربط/إلغاء ربط بنقرة واحدة
- عداد واضح للمزارع المرتبطة

---

## 🚀 الخطوات القادمة (المرحلة 3)

### اقتراحات للتوسع:

1. **إشعارات مدير المزرعة:**
   - إشعار عند إضافة مهمة جديدة
   - إشعار عند اقتراب موعد مهمة
   - إشعار عند اكتمال مهمة

2. **تقارير مدير المزرعة:**
   - تقرير أداء المزرعة
   - تقرير المهام المكتملة
   - تقرير الإنتاجية

3. **نظام الموافقات:**
   - طلبات الصيانة تحتاج موافقة مدير المزرعة
   - طلبات المعدات تحتاج موافقة
   - المصروفات الكبيرة تحتاج موافقة

4. **تبويب الصيانة:**
   - جدولة الصيانة الدورية
   - مدير المزرعة يعتمد أعمال الصيانة
   - تتبع تكاليف الصيانة

---

## ✅ الخلاصة

المرحلة الثانية من قسم "محصولي" مكتملة بنجاح! النظام الآن يوفر:

### ما تم إنجازه:
- ✅ ربط مدير المزرعة بمزارع محددة
- ✅ واجهة إدارة سهلة وبديهية
- ✅ عرض المزارع المرتبطة فقط
- ✅ صلاحيات دقيقة لإغلاق المهام
- ✅ حماية أمنية شاملة
- ✅ لا فوضى - لا تداخل

### النتيجة النهائية:
**مدير المزرعة هو المشغل الفعلي لمزارعه فقط!**

النظام جاهز للاستخدام الفوري ومتكامل مع المرحلة 1!
